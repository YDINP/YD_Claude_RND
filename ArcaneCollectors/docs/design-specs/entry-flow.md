# 진입 플로우 디자인 스펙

> DESIGN_SYSTEM.md 기반 | 1080x1920 (9:16) | 다크 테마 전용

---

## 1. BootScene (스플래시 화면)

### 1.1 레이아웃

```
┌─────────────────────────────┐ 0
│                             │
│     ✦  ·   ✦    ·    ✦    │ 별 파티클 (20개)
│         ·    ✦   ·         │
│                             │
│        ╭──────────╮         │ y = 40% (마법진)
│       ╱  ○ 마법진 ○ ╲       │ r=100, r=80
│        ╰──────────╯         │
│                             │
│         A R C A N E         │ y = 40% - s(20)
│        COLLECTORS           │ y = 40% + s(20)
│                             │
│   신화의 교단에서 영웅을 모아라  │ y = 40% + s(48)
│                             │
│                             │
│                             │
│      YD Studio © 2025       │ y = HEIGHT - s(40)
└─────────────────────────────┘ 1920
```

### 1.2 컬러 스펙

| 요소 | 현재값 | 디자인 시스템 | 변경 |
|------|--------|-------------|------|
| 배경 | `0x0a0a1a` | `0x0F172A` (bgDark) | O |
| 별 파티클 | `0x6366f1` | `0x6366F1` (primary) | - (동일) |
| 마법진 외곽 | `0x6366f1` | `0x6366F1` (primary) | - |
| 마법진 내부 | `0xEC4899` | `0xEC4899` (secondary) | - |
| 타이틀 "ARCANE" | `#6366F1` | `#6366F1` (primary) | - |
| 서브타이틀 "COLLECTORS" | `#a5b4fc` | `#a5b4fc` (primary light) | - |
| 설명문 | `#64748b` | `#94A3B8` (textDark) | O |
| 저작권 | `#334155` | `#94A3B8` (textDark) alpha 0.5 | O |
| 에러 텍스트 fill | `#ff4444` | `#EF4444` (danger) | O |

### 1.3 타이포그래피 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| "ARCANE" | `52px Georgia` | `sf(32)` H1 `Noto Sans KR` bold | O |
| "COLLECTORS" | `28px Georgia` | `sf(24)` H2 `Noto Sans KR` | O |
| 설명문 | `13px Arial` | `sf(14)` Caption `Noto Sans KR` | O |
| 저작권 | `11px Arial` | `sf(12)` Small `Noto Sans KR` | O |
| 에러 텍스트 | `20px` | `sf(18)` Body `Noto Sans KR` | O |

### 1.4 간격 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| 로고 Y | `GAME_HEIGHT * 0.4` | 유지 (40%) | - |
| ARCANE ↔ COLLECTORS 간격 | 55px (하드코딩) | `s(20)` | O |
| COLLECTORS ↔ 설명문 간격 | 40px (하드코딩) | `s(24)` | O |
| 저작권 Y | `GAME_HEIGHT - 60` | `GAME_HEIGHT - s(40)` | O |
| 별 여백 | `50 ~ WIDTH-50` | `s(20) ~ WIDTH-s(20)` | O |

### 1.5 애니메이션 스펙

| 애니메이션 | 현재 | 디자인 시스템 | 변경 |
|-----------|------|-------------|------|
| 타이틀 입장 | 600ms Back.easeOut | 유지 (커스텀 브랜딩) | - |
| 서브타이틀 입장 | 600ms/200ms Quad | 400ms Power2 | O |
| 설명문 입장 | 400ms/500ms Quad | 400ms/500ms Power2 | O |
| 카메라 페이드아웃 | 500ms | 200ms Power2 (씬 규격) | O |
| 스플래시 대기 | 2400ms | 2400ms (유지) | - |

---

## 2. LoginScene (로그인 화면)

### 2.1 레이아웃

```
┌─────────────────────────────┐ 0
│                             │
│     ✦  ·   ✦    ·    ✦    │ 별 파티클 (30개)
│                             │
│         A R C A N E         │ y = 30% - s(20)
│        COLLECTORS           │ y = 30% + s(20)
│   신화의 교단에서 영웅을 모아라  │ y = 30% + s(48)
│                             │
│                             │
│  ┌──────────────────────┐   │ y = 60%
│  │   ⚔ 게스트로 시작    │   │ Primary 버튼 s(280)xs(52)
│  └──────────────────────┘   │
│  ┌──────────────────────┐   │ y = 60% + s(70)
│  │   ✉ 이메일 로그인    │   │ Secondary 버튼 s(280)xs(44)
│  └──────────────────────┘   │
│  ┌──────────────────────┐   │ y = 60% + s(125)
│  │   📝 회원가입        │   │ Secondary 버튼 s(280)xs(44)
│  └──────────────────────┘   │
│                             │
│     ☑ 자동 로그인           │ y = HEIGHT - s(60)
│                             │
│   v1.0.0 | Supabase ON     │ y = HEIGHT - s(20)
└─────────────────────────────┘ 1920
```

### 2.2 컬러 스펙

| 요소 | 현재값 | 디자인 시스템 | 변경 |
|------|--------|-------------|------|
| 배경 | `0x0a0a1a` | `0x0F172A` (bgDark) | O |
| 별 파티클 | `0x6366f1` | `0x6366F1` (primary) | - |
| 메인 버튼 | `COLORS.primary` | 유지 | - |
| 메인 버튼 스트로크 | `0x4f46e5` | `COLORS.primary` alpha 0.3 | O |
| 서브 버튼 | `0x374151` | `0x1E293B` (bgLight) | O |
| 버튼 텍스트 | `#ffffff` | `#F8FAFC` (text) | O |
| 체크박스 배경 | `0x1e293b` | `0x1E293B` (bgLight) | - |
| 체크박스 스트로크 | `COLORS.primary` | 유지 | - |
| 체크표시 | `#6366F1` | `#6366F1` (primary) | - |
| 체크라벨 | `#94a3b8` | `#94A3B8` (textDark) | - |
| 버전 정보 | `#475569` | `#94A3B8` (textDark) alpha 0.5 | O |
| 토스트 배경 | `#1e1b4b` | `0x334155` (bgPanel) | O |
| 토스트 텍스트 | `#fca5a5` | `#EF4444` (danger) alpha 0.8 | O |
| 폼 배경 | `0x1e1b4b` | `0x1E293B` (bgLight) | O |
| 폼 스트로크 | `0x6366f1` | `COLORS.primary` alpha 0.5 | O |

### 2.3 타이포그래피 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| "ARCANE" | `sf(48) Georgia` | `sf(32)` H1 `Noto Sans KR` bold | O |
| "COLLECTORS" | `sf(28) Georgia` | `sf(24)` H2 `Noto Sans KR` | O |
| 설명문 | `sf(13) Arial` | `sf(14)` Caption `Noto Sans KR` | O |
| 버튼 텍스트 | `16px Arial` | `sf(16)` Body Small `Noto Sans KR` bold | O |
| 체크라벨 | `14px Arial` | `sf(14)` Caption `Noto Sans KR` | O |
| 체크표시 | `16px Arial` | `sf(14)` `Noto Sans KR` bold | O |
| 폼 제목 | `20px Arial` | `sf(20)` H3 `Noto Sans KR` bold | O |
| 버전 정보 | `sf(10) Arial` | `sf(10)` Tiny `Noto Sans KR` | O |
| 토스트 | `13px Arial` | `sf(14)` Caption `Noto Sans KR` | O |
| 닫기 버튼 | `20px` | `sf(20)` `Noto Sans KR` | O |
| 에러 텍스트 | `sf(20)` | `sf(18)` Body `Noto Sans KR` | O |

### 2.4 간격 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| 타이틀 Y | `30%` | 유지 | - |
| ARCANE ↔ COLLECTORS | `s(30)` x2 = `s(60)` | `s(20)` x2 = `s(40)` | O |
| COLLECTORS ↔ 설명문 | `s(40)` | `s(24)` | O |
| 버튼 시작 Y | `60%` | 유지 | - |
| 게스트 ↔ 이메일 | `s(70)` | `s(70)` (유지) | - |
| 이메일 ↔ 회원가입 | `s(55)` | `s(55)` (유지) | - |
| 체크박스 Y | `HEIGHT - 80` | `HEIGHT - s(60)` | O |
| 체크박스 크기 | `20px` | `s(16)` | O |
| 버전 정보 Y | `HEIGHT - s(30)` | `HEIGHT - s(20)` | O |

### 2.5 버튼 스펙

| 속성 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| 메인 버튼 크기 | `s(280) x s(52)` | `s(280) x s(60)` (규격 높이) | O |
| 서브 버튼 크기 | `s(280) x s(44)` | `s(280) x s(48)` (최소 터치) | O |
| 모서리 | 직각 (0) | `s(8)` 라운드 | O |
| 호버 | brighten(20) | alpha 0.8 (규격) | O |
| 다운 | 없음 | scale 0.95 | O |

### 2.6 애니메이션 스펙

| 애니메이션 | 현재 | 디자인 시스템 | 변경 |
|-----------|------|-------------|------|
| 카메라 Fade In | 400ms | 400ms Power2 (씬 Fade In 규격) | - |
| 카메라 Fade Out | 300ms | 200ms Power2 (씬 Fade Out 규격) | O |
| 토스트 | 2000ms/1500ms delay | 2000ms 대기 + 150ms 페이드 (규격) | O |

---

## 3. PreloadScene (데이터 로딩)

### 3.1 레이아웃

```
┌─────────────────────────────┐ 0
│                             │
│                             │
│                             │
│          ○ ○ ○ ○           │ y = 38% (마법진)
│        ○  ╭───╮  ○         │ 외곽링 r=60
│        ○  │ ● │  ○         │ 내부링 r=40 + 글로우 r=15
│        ○  ╰───╯  ○         │
│          ○ ○ ○ ○           │ 도트 8개
│                             │
│                             │
│     [Phase 텍스트]          │ y = 58% - s(32)
│     에셋 준비 중...         │ y = 58% - s(16)
│     ╔══════════════╗        │ y = 58%
│     ║████████░░░░░░║        │ 320 x 12 진행바
│     ╚══════════════╝        │
│          45%                │ y = 58% + s(20)
│                             │
│                             │
└─────────────────────────────┘ 1920
```

### 3.2 컬러 스펙

| 요소 | 현재값 | 디자인 시스템 | 변경 |
|------|--------|-------------|------|
| 배경 | `0x0a0a1a` | `0x0F172A` (bgDark) | O |
| 외곽 링 | `0x6366f1` alpha 0.6 | `0x6366F1` (primary) alpha 0.6 | - |
| 내부 링 | `0xEC4899` alpha 0.4 | `0xEC4899` (secondary) alpha 0.4 | - |
| 도트 | `0x6366f1` alpha 0.7 | `0x6366F1` (primary) alpha 0.7 | - |
| 글로우 | `0x6366f1` alpha 0.2 | `0x6366F1` (primary) alpha 0.2 | - |
| 진행바 배경 | `0x1e293b` | `0x1E293B` (bgLight) | - |
| 진행바 채움 | `0x6366f1` | `0x6366F1` (primary) | - |
| 로딩 텍스트 | `#a5b4fc` | `#F8FAFC` (text) alpha 0.8 | O |
| Phase 텍스트 | `#64748b` | `#94A3B8` (textDark) | O |
| 퍼센트 텍스트 | `#64748b` | `#94A3B8` (textDark) | O |
| 에러 텍스트 fill | `#ff4444` | `#EF4444` (danger) | O |

### 3.3 타이포그래피 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| 로딩 텍스트 | `16px Arial` | `sf(16)` Body Small `Noto Sans KR` | O |
| Phase 텍스트 | `11px Arial` | `sf(12)` Small `Noto Sans KR` | O |
| 퍼센트 텍스트 | `13px Roboto Mono` | `sf(14)` Caption `Noto Sans KR` | O |
| 에러 텍스트 | `20px` | `sf(18)` Body `Noto Sans KR` | O |

### 3.4 간격 스펙

| 요소 | 현재 | 디자인 시스템 | 변경 |
|------|------|-------------|------|
| 마법진 Y | `38%` | 유지 | - |
| 로딩텍스트 ↔ 진행바 | `25px` | `s(16)` | O |
| Phase텍스트 ↔ 로딩텍스트 | `23px` | `s(16)` | O |
| 퍼센트 ↔ 진행바 | `16px + barHeight` | `s(20)` | O |
| 진행바 크기 | `320 x 12` | `s(200) x s(8)` (ProgressBar 규격 스케일) | O |

### 3.5 애니메이션 스펙

| 애니메이션 | 현재 | 디자인 시스템 | 변경 |
|-----------|------|-------------|------|
| 카메라 Fade In | 400ms | 400ms Power2 (규격) | - |
| 카메라 Fade Out | 400ms | 200ms Power2 (규격) | O |
| 외곽 링 회전 | 3000ms Linear | 유지 (커스텀) | - |
| 내부 링 역회전 | 2000ms Linear | 유지 (커스텀) | - |
| 글로우 펄스 | 1000ms Sine | 유지 (커스텀) | - |

---

## 4. 씬 전환 플로우

```
BootScene ──[2.4s + 200ms fadeout]──> LoginScene (세션 없음)
    │                                      │
    │                              [200ms fadeout]
    │                                      │
    └──[2.4s + 200ms fadeout]──> PreloadScene (세션 있음)
                                           │
                                   [200ms fadeout]
                                           │
                                     MainMenuScene
```

### 전환 규격 (DESIGN_SYSTEM.md 6.2)

- **씬 Fade Out**: 200ms, Power2 ease
- **씬 Fade In**: 400ms, Power2 ease
- **Fade 색상**: `0x0F172A` (bgDark) = RGB(15, 23, 42)

---

## 5. 공통 패턴

### 5.1 별 파티클

모든 진입 씬에서 공유하는 장식 패턴:
- 색상: `0x6366F1` (primary)
- 크기: 1~3px
- 알파: 0.1~0.5
- 트윈: Sine.easeInOut, 1500~3000ms, yoyo, repeat -1

### 5.2 타이틀 로고

BootScene과 LoginScene 공유:
- "ARCANE": `sf(32)`, `Noto Sans KR`, bold, `#6366F1`
- "COLLECTORS": `sf(24)`, `Noto Sans KR`, `#a5b4fc`, letterSpacing `s(4)`
- 설명문: `sf(14)`, `Noto Sans KR`, `#94A3B8`

### 5.3 에러 폴백

모든 씬 공통:
- 폰트: `sf(18)`, `Noto Sans KR`, `#EF4444`
- 2000ms 후 다음 씬으로 이동
