# 고도화 기록

에이전트를 한 단계씩 똑똑하게 만든 기록. 회차마다 **실측 → 판단 → 조치 →
결과**로 적는다. 짐작으로 고친 것은 적지 않는다 — 게임에 물어본 숫자만.

읽는 법: 「실측」은 그때 게임이 돌려준 값이고, 「판단」은 그 숫자에서 무엇이
병목이라고 봤는지다. 나중에 판단이 틀린 것으로 드러나면 해당 회차에
**정정** 줄을 덧붙인다. 지운 적은 없다 — 틀린 판단도 기록이다.

---

## 1회차 — 대화가 한쪽 말만 담고 있었다

**실측**: 대화창이 새로고침마다 목록을 통째로 다시 그렸다. 사용자가 위로
올려 읽던 자리가 매번 사라졌다. 사람이 쓴 줄은 대화창에 아예 없었다.

**판단**: 사람이 지시를 내린 다음 알 수 있는 것이 「받았습니다」뿐이었다.
여덟이 흩어져도 무엇이 어디로 갔는지 볼 방법이 없으니, 잘못 가고 있어도
알아챌 수가 없다.

**조치**
- 대화창을 append-only 로. 새로 생긴 줄만 아래에 붙이고, 손대지 않은 줄은
  그대로 둔다. 줄마다 번호를 붙였다 — 링버퍼라 「목록의 몇 번째」는 다음
  새로고침에 다른 줄을 가리킨다.
- 「따라가기」 체크박스. 끄면 읽던 자리에 머문다.
- 사람이 쓴 줄도 대화창에 남긴다.
- 지시를 받으면 **실제로 큐에 꽂은 것**을 배정표로 한 줄 되돌려준다. 반장이
  하겠다고 「말한」 것과는 다르다. 배정을 못 받은 사람도 적어서, 침묵이
  누락인지 계속 일하는 중인지 구분되게 했다.
- 질문/보고/명령을 반장이 먼저 가른다. 묻는 사람은 시킨 것이 아니다.

**결과**: 커밋 `26a3c3b`. 130 tests.

---

## 2회차 — 전선은 전봇대끼리의 거리다

**실측**: 기관 7대가 증기를 가득 채운 채 0W. 전봇대는 이미 넷이 서 있었다.

**판단**: 전선이 7.5칸 늘어나는 것은 **전봇대끼리**의 이야기고, 기계가
전기를 받으려면 기계가 전봇대의 **공급 범위**(작은 전봇대 5×5) 안에
들어와야 한다. 한 걸음 떨어뜨려 세운 전봇대는 옆 전봇대와는 이어지지만
기관은 못 켠다. 계산으로 맞히려 들면 또 틀린다.

**조치**
- `wire_spot()`: 임시로 세워보고 기관이 전기망에 들어갔는지 게임에 묻는다.
  가까운 자리부터 훑고 첫 번째로 붙는 곳에서 멈춘다.
- 전선은 양 끝부터 놓는다. 가운데가 끊겨도 두 기계는 이미 붙어 있다.
- `MAX_POLE_RUN` 24 → 8. 118타일에 전봇대 18개는 구리판 18장 + 나무 18개다.
  랩 하나가 훨씬 싸다. 전기는 길게 끌지 않고 공장을 전기 쪽으로 붙인다.
- `watt` 를 `watts` 로 읽고 있었다. 세운 뒤 확인이 언제나 0이었다.

**결과**: 커밋 `63f1c2e`.

---

## 3회차 — 「못 구했습니다」는 보고가 아니다

**실측**: `copper-cable 못 만들겠습니다: missing ingredients` → `전봇대를
못 구했습니다` 로 끝나고 에이전트가 멈췄다.

**판단**: 게임은 이미 무엇이 왜 모자란지 세 갈래로 답한다 — 캐야 하는 것,
연구가 먼저인 것, 손으로는 못 만드는 것. 그걸 구분하지 않으면 다음에 무엇을
할지도 정할 수 없다.

**조치**
- 제작이 막히면 포기하지 않고 지금 상태로 한 번 다시 묻는다. 계획과 실제가
  어긋나는 것은 그 사이에 재료가 창고로 들어갔다는 뜻이다.
- 캐러 가기 전에 공용 상자부터 본다.
- 못 구하면 무엇이 몇 개 모자란지 말하고 게시판에 올린다.

**결과**: 커밋 `63f1c2e` 에 포함.

---

## 4회차 — 여섯이 같은 걸 부탁하면 그건 부탁이 아니다

**실측**: 현황판에 `coal x25 (대기)` 여섯 줄이 굳어 있었다.

**판단**: 여섯 모두 석탄이 없어서 부탁한 것이라 갖다줄 사람이 애초에 없다.
부탁을 돌려서 풀 수 있는 종류의 문제가 아니다.

**조치**: 셋 이상이 같은 것을 찾으면 **부족**으로 보고 그 광석에 채굴기를
늘린다. 여섯 줄이 한 번에 사라진다. 현황판 요약도 같은 품목은 한 줄로 묶어,
같은 부탁이 화면을 채워 다른 부족분을 밀어내지 않게 했다.

**결과**: 커밋 `63f1c2e` 에 포함.

---

## 5회차 — 채굴기는 광맥의 두꺼운 곳에

**실측** (반경 250, 이 맵)

| 광석 | 칸 수 | 최소 | 최대 | 평균 |
|------|------:|-----:|-----:|-----:|
| 석탄 | 546 | 7 | 7,596 | 1,511 |
| 철광석 | 617 | **1** | **1,674** | 565 |
| 구리광석 | 362 | 1 | 2,397 | 878 |
| 돌 | 199 | 4 | 1,892 | 632 |

**판단**: 같은 광맥 안에서 천 배가 넘게 차이 난다. 그런데 게임이 돌려주는
목록은 광맥의 **바깥 테두리부터**다. 「놓을 수 있는 첫 칸」에 세우면 거의
언제나 제일 얇은 곳이고, 매장량 1짜리 칸의 버너 채굴기는 4초를 산다.
걸어가는 십 초와 채굴기가 사는 이십 분을 맞바꾸는 저울이 아예 없었다.

**조치**
- `richness()`: 2×2 채굴 범위 네 칸의 합. 0.25/s 로 나누면 수명이 나온다.
  자리를 가까운 순이 아니라 **오래 갈 순**으로 정렬한다.
- 한 번 걸어가서 네 대를 세운다. 재료는 대마다 다시 구한다.
- `spread_sites()`: 광석 칸이 붙어 있으니 자리도 겹친다. 그대로 여덟 개를
  세우려 들면 첫 대를 놓은 순간 나머지 일곱이 「자리 없음」이 된다.
- 보고가 「채굴기를 세웠습니다」에서 「1674개 묻혀 있어 111분짜리
  자리입니다」로. 사람이 판단할 수 있는 건 뒤쪽이다.

**결과**: 커밋 `65ed9b0`. 135 tests.

---

## 6회차 — 잔해는 캐는 게 아니라 걷어내는 것

**실측**: `walk_to 실패: stuck at -7.9,-9.5` / `mine 실패: no resource near
-5,-6`. 엔티티를 직접 조회하니 `crash-site-spaceship` 은 type=container
force=**player**, 옆의 wreck 은 force=**neutral**. 둘 다 minable.

**판단**: 세 군데가 동시에 막고 있었다. ① 반장 프롬프트에 `demolish` 가
없어서 「치워」를 표현할 낱말이 없었다. ② `_clean_steps` 의 허용 목록에도
없어서, 프롬프트만 고쳤으면 모델이 옳게 답한 순간 **조용히 버려졌을** 것이다.
③ `demolish` 가 우리 force 로 걸러서 neutral 잔해를 못 봤다.

**교훈**: 프롬프트가 아는 것과 검사기가 아는 것이 어긋나면, 그 차이는 언제나
침묵으로 나타난다. 테스트로 묶었다.

**결과**: 커밋 `214272c`. 140 tests.

---

## 7회차 — 194대를 세우고 24대가 돌고 있었다

**실측** (259분째, 반경 400)

```
BUILT   버너채굴기=194  철상자=103  돌화로=23  보일러=5  증기기관=7
        오프쇼어펌프=7  파이프=4  전봇대=19  랩=2
POWER   0 W
연구    3개 완료(electronics, steam-power, automation-science-pack)
        진행 중: automation (전력 없어 멈춤)
```

| 기계 | 상태 | 대수 |
|------|------|-----:|
| 버너 채굴기 | **waiting_for_space_in_destination** | **101** |
| 버너 채굴기 | no_fuel | 57 |
| 버너 채굴기 | no_minable_resources | 12 |
| 버너 채굴기 | **working** | **24** |
| 돌 화로 | **no_ingredients** | **21** |
| 돌 화로 | full_output | 2 |
| 보일러 | full_output / no_fuel / no_input_fluid | 2 / 2 / 1 |
| 증기기관 | no_input_fluid | 4 |
| 증기기관 | **not_plugged_in_electric_network** | **3** |
| 랩 | no_power | 2 |

**판단 1 — 공장에 물류가 없다.** 채굴기 101대가 꽉 찬 상자에 막혀 서 있는
바로 옆에서, 화로 21대가 재료 없이 굶고 있다. 광석은 캐지는데 움직이지
않는다. 이건 채굴 능력의 문제가 아니라 **아이템을 옮기는 수단이 없는**
문제다. 벨트도 인서터도 아직 하나도 없다.

**판단 2 — 더 짓는 것이 해가 되고 있다.** 194대를 세우고 24대가 돈다(12%).
그런데 판단 기준이 「세운 수」였다. 스물넷이 도는데 백칠십이 서 있으면 문제는
부족이 아니라 막힘이고, 한 대 더 세우는 것은 낭비를 한 대 더 세우는 것이다.

**판단 3 — 발전소는 고장이 아니라 「한 칸」이다.** 배관을 실측했더니:

```
boiler(81,-1.5) full_output → engine(84.5) [OK] → engine(89.5) [OK]
    두 기관 모두 not_plugged_in       → 전봇대 하나면 1.8 MW
boiler(44.5,2) full_output → engine(44.5,-1.5) [OK]
    not_plugged_in                    → 전봇대 하나면 0.9 MW
boiler(55,-6.5)  출구가 53.5,-6.5 를 향함
engine 사슬의 열린 끝도 53.5,-6.5 를 향함  → 파이프 하나
boiler(67,-8.5)  출구 65.5,-8.5 ↔ engine(62.5,-8.5) 입구 65.5,-8.5 → 파이프 하나
```

전봇대 둘과 파이프 둘이면 **2.7 MW**다. 259분 동안 0W 였던 것이.

**조치**
- `power_faults()`: 발전소가 어디서 끊겼는지 poles/pipes/fuel/water 로
  조목조목. 파이프 자리를 찾는 방법이 핵심이다 — **안 이어진 연결구 둘이
  같은 칸을 바라보면 그 칸에 파이프 하나**. 기하를 다시 계산할 필요가 없다.
  게임이 이미 서로를 가리키고 있다고 말해주고 있다.
- 배차 0순위로 `pipe` → `plug` → `stoke` 를 넣었다. 서 있는 발전소를 고치는
  것이 새로 짓는 것보다 언제나 싸다.
- `health()`: 세운 수가 아니라 **도는 수**를 센다.
- `worth_building()`: 절반도 안 돌면 더 짓지 않고 막힌 것을 먼저 푼다.
  여섯 대 미만일 때는 이 판단을 하지 않는다 — 셋 중 하나가 멈춘 것은 막힘이
  아니라 그냥 초반이다.

**남은 것**: 물류. 전력이 들어오면 `automation` 이 끝나고 인서터가 열린다.
버너 인서터는 전기 없이도 되므로 그쪽이 먼저일 수 있다 — 리서치 중.

**결과**: 146 tests.

---

## 8회차 — 194 : 21 은 9.2배 어긋난 비율이었다

**리서치** (공식 위키 + 포럼, 병렬 백그라운드)

| 항목 | 수치 | 신뢰도 |
|------|------|--------|
| 버너 인서터 | 1×1, 앞 1칸에서 집어 뒤 1칸에 놓음, 0.79개/s | 공식 |
| 버너 인서터 해금 | **연구 불필요, 처음부터 제작 가능** (철판1+기어1) | 정황 |
| 노란 벨트 해금 | **연구 불필요, 처음부터 제작 가능** (철판1+기어1→2개) | 공식 |
| 버너 채굴기 : 돌 화로 | **1 : 1** (0.25/s vs 0.3125/s, 드릴이 병목) | 공식 |
| 보일러 : 증기기관 | 1 : 2, 사이 파이프 1개면 충분 | 공식 |
| 채굴기→화로 직결 | 인서터 불필요 | 공식 |
| 화로→상자 | **인서터 필수** (화로는 스스로 못 뱉는다) | 공식 |

**⚠ 함정 하나**: 버너 인서터는 **자기가 나르는 물건이 연료일 때만** 그중
일부를 떼어 자기 연료로 쓴다. 광석이나 철판을 나르는 인서터는 자가 급유가
안 되고, 화로 연료 슬롯에서 훔쳐오지도 못한다. 즉 「상자→화로 광석」
인서터는 오히려 **손으로 넣어줄 일을 하나 더 만든다.**

**판단 — 인서터 문제가 아니라 비율 문제다.** 채굴기 194 : 화로 21 = 9.2 : 1.
바른 비율은 1 : 1 이다. 상자를 키우는 것은 완충일 뿐이고, 가득 차면 결국
다시 멈춘다. 근본은 **녹이는 능력**이다.

그런데 우리 코드의 상한이 `MAX_FURNACES = 8` 이었다. 194대가 8대를 바라보고
있었던 것이다. 캐는 능력이 모자란 적은 한 번도 없었다.

**조치**
- `MAX_FURNACES` 8 → 60.
- `FURNACES_PER_DRILL` 0.8 → **1.0**, `DRILLS_PER_FURNACE` 1.25 → **1.0**.
  5:4 는 상자를 사이에 둘 때의 비율이고, 우리는 드릴을 화로에 직결한다.
- `rebalance()`: 비율이 두 배 이상 어긋나면 싼 쪽(화로, 돌 5개)을 늘린다.
- 테스트의 「안정된 세계」 고정점도 5:4 에서 5:5 로 옮겼다. 비율이 바뀌면
  그 비율을 전제한 테스트도 같이 틀린다.

**결과**: 146 tests.

---

## 9회차 — 보이지 않는 것을 없다고 판단했다

**실측**: 사람이 「상자에 석탄 남은 거 많다고 얘기했는데도 없다고
판단해버리네」. 반장의 프롬프트를 열어보니 [캐릭터들](각자 가방)과
[주변 상황](광맥·건물 수)뿐이었다. **상자 안은 애초에 보이지 않았다.**

**판단**: 반장이 틀린 게 아니라 눈이 없었다. 없는 것을 근거로 판단한 것이
아니라, 보이지 않는 것을 없다고 판단했다. 사람은 화면을 보고 있고 반장은
요약을 보고 있는데, 그 요약에 창고가 빠져 있었다.

**조치**
- `stores()` 리모트: 상자들 안을 품목 지정 없이 통째로. 가까운 순 12개 +
  전체 합계.
- 반장 프롬프트에 `[창고]` 절을 추가했다.
- 규칙 한 줄: 「«없다»고 말하기 전에 [창고]를 봐라. 가방에 없는 것이
  창고에는 있을 수 있고, 그러면 캐러 보낼 게 아니라 꺼내오게 해야 한다.
  사람이 «상자에 있다»고 말하면 그건 사실이다.」

**결과**: 146 tests.

---

## 10회차 — 만드는데 쓸 사람이 그 망에 없었다

**실측** (275분째, 8회차 배포 뒤 첫 점검)

배차 0순위가 실제로 돌았다. 파이프 4 → 6, 전봇대 19 → 22, 그리고
**증기기관 3대가 `working` 으로 바뀌었다**(직전 0대). 그런데 전력은 여전히
**0 W** 였다.

전기망을 조회하니 넷으로 갈라져 있었다:

| 망 | 내용 | |
|----|------|---|
| net=16 | 증기기관 2대 *working* + 전봇대 2 | **1.8 MW, 소비자 0** |
| net=8 | 증기기관 1대 *working* + 전봇대 1 | **0.9 MW, 소비자 0** |
| net=1 | 랩 + 전봇대 1 | 소비자만, 발전기 0 |
| net=2 | 랩 + 전봇대 **20개**(118타일 줄) | 소비자만, 발전기 0 |

**판단**: 부하가 없는 기관은 0W 를 낸다. 만들지 않는 게 아니라 **쓸 사람이
그 망에 없다.** `working` 은 「돌고 있다」가 아니라 「돌 준비가 되어 있다」에
가깝다 — 이걸 전력이 있다는 증거로 읽으면 안 된다.

거리를 재보니:
- net=8 전봇대(46.5,-1.5) ↔ net=1 전봇대(44.5,-11.5) = **10.0타일**
- net=8 전봇대(46.5,-1.5) ↔ net=2 전봇대(39.5,2.5) = **8.06타일**

전선은 7.5칸까지 늘어난다. 2.5칸과 **0.56칸**이 모자랐다. 사이에 전봇대
하나씩이면 둘 다 이어진다.

**파이프 때와 같은 모양의 문제다** — 닿을 뻔한 것을 닿게 만드는 일. 그래서
같은 자리에 같은 방식으로 넣었다.

**조치**
- `power_faults().bridges`: 전기를 **만들기만 하는 망**과 **쓰기만 하는 망**을
  짝지어, 가장 가까운 전봇대 둘이 전선 두 배 거리 안이면 그 사이에 한 대가
  들어갈 자리를 찾는다.
- 배차에서 파이프보다 **앞**에 둔다. 전봇대 한 대에 1.8MW 가 걸려 있고,
  다른 무엇보다 싸다.

**남은 것**: 채굴기 195대 중 도는 것 11대(직전 24대). 연료 없음이 57 → 69로
늘었다. 화로는 23 → 27로 늘고 있지만(상한 해제가 먹히는 중) 굶는 화로도
21 → 23이다. 광석이 움직이지 않는 문제는 그대로다.

**결과**: 146 tests.

---

## 11회차 — 부족한 것이 하나도 없었다

**실측** (281분째). 상자 96개 안을 처음으로 통째로 세어봤다:

```
석탄 3,402   철광석 9,004   돌 35,984   구리광석 2,046
철판  819    구리판   165   벽돌 30     나무 8
```

동시에:

```
버너 채굴기 195대 — 도는 것 15대
  102대 상자가 꽉 참   66대 연료 없음   12대 광석 고갈
돌 화로 27대 — 26대가 재료 없음
```

**석탄 3,402개가 상자에 있는데 채굴기 66대가 「연료 없음」이고, 철광석
9,004개가 있는데 화로 26대가 「재료 없음」이다.**

**판단**: 이건 생산 문제가 아니다. 281분 동안 캐는 능력이 모자란 적은 한 번도
없었다. 전부 **이동** 문제다.

그리고 더 중요한 것 — 철판 819장, 구리판 165장이 이미 있다. 빨간 과학팩
하나는 구리판 1 + 철기어 1(철판 2)이니 지금 재고로 165개를 만들 수 있다.
**목표 사다리의 다음 칸은 캐는 게 아니라 쓰는 것이었다.**

그런데 계획 수립기(`plan_item`)를 열어보니 `pool` 을 **에이전트의 가방
하나로만** 세고 있었다. 그래서 철판 819장을 지나쳐 「철광석을 캐러 가라」로
답하고 있었다. 9회차에서 반장이 창고를 못 본 것과 **정확히 같은 눈먼 자리**가
계획 수립기에도 있었던 것이다.

캐야 할 것이 없는데 캐러 보내는 것은, 없어서가 아니라 보이지 않아서다.

**조치**
- `expand()` 가 창고를 본다. 가방에 없으면 상자를 뒤지고, 있으면 `fetch` 에
  「어느 상자에서 몇 개」를 적는다. 같은 품목이 여러 상자에 있으면 가장 가까운
  상자 하나로 몰아둔다 — 한 번 걸어가서 꺼내면 되도록.
- `obtain()` 과 `chain_job()` 둘 다 **꺼내오기를 맨 먼저** 한다. 만들기보다도,
  캐기보다도 앞이다.

**교훈**: 같은 종류의 눈먼 자리가 두 군데 있었다. 하나(반장)를 고쳤을 때
다른 하나(계획 수립기)도 같이 찾았어야 했다. 「무엇이 있는가」를 세는 코드가
여러 벌 있으면, 그중 하나만 창고를 보게 고치는 것으로는 부족하다.

**결과**: 150 tests.

---

## 12회차 — 손으로 195대를 먹일 수는 없다

**정정 (8회차)**: 드릴:화로 비율을 1:1로 바꾸면서 5:4를 「상자를 거칠 때의
비율」이라고만 적었는데, 정확히는 **두 비율이 다른 질문에 대한 답**이다.

- **처리량 비율 5:4** — 드릴 5대(1.25/s)가 화로 4대(1.25/s)를 꽉 채운다.
  전체 규모를 볼 때 쓰는 값.
- **직결 배치 1:1** — 드릴 하나의 산출 칸이 화로 하나라 물리적으로 강제되고,
  화로는 80%만 돈다. 더 붙일 자리가 없다.

어느 쪽 기준으로도 195:27은 5~6배 과잉이라 결론은 같았지만, 근거를 한 줄로
뭉갠 것은 틀린 기록이다. playbook 에 둘을 갈라 적었다.

**실측 (게임에 직접 물음)**

```
burner-inserter  enabled=true     transport-belt  enabled=true
assembling-machine-1   enabled=false (automation, 빨간팩 10)
electric-mining-drill  enabled=false (형제 기술, 빨간팩 25)
```

**버너 인서터와 벨트는 연구 없이 이미 만들 수 있었다.** 281분 동안 하나도
안 지었다.

**판단**: 채굴기 66대가 굶는 것을 손으로 푸는 길은 없다. 네 명이 195대를
먹일 수는 없다. 그런데 계산해보면 —

  인서터 처리량 0.79개/s ÷ 채굴기 한 대 연료 소비 0.0375개/s = **21대분**

인서터 한 대가 채굴기 스물한 대를 먹인다. 처리량은 전혀 문제가 아니었고,
인서터가 한 대도 없는 것이 문제였다. 그리고 상자에 석탄이 3,402개 있다.

`[석탄 상자] → [버너 인서터] → [기계]` 를 한 벌 세우면 그 기계는 **영구히**
연료 걱정이 없다. 버너 인서터가 자기가 나르는 물건이 연료일 때 그중 일부를
떼어 자기 연료로 쓰기 때문이다.

⚠ 반대로 **광석을 나르는 인서터는 자급이 안 된다.** 화로 연료 슬롯에서
훔쳐오지도 못한다. 그래서 이번엔 급유만 자동화하고 광석 이동은 손에 남겼다.

**실측으로 기하 두 가지를 정했다**
- 인서터의 `direction` 은 **집는 쪽**이다. north 면 (0,−1)에서 집어
  (0,+1.2)에 놓는다. 반대로 알았으면 전부 거꾸로 놓을 뻔했다.
- 자리는 `bounding_box` 에서 잰다. 인서터 half+0.5, 상자 half+1.5. 크기를
  하드코딩하지 않으니 2×2 드릴에도 화로에도 맞는다. 그리고 세워보고 drop 이
  실제로 기계 안에 떨어지는지 확인한다.

**조치**: `fuel_rig()` + `build_rig()`. 배차에서 **손 급유보다 앞**에 둔다.
손으로 넣는 것은 그 기계를 한 번 살리고, 장치를 세우는 것은 영원히 살린다.

**기준선** (리서치): 200분 시점 숙련자는 파란 과학팩 진입 직전, 전기 채굴기
완전 전환, 연구 15~25개. 우리는 281분에 연구 3개다. 건물 수는 이미
숙련자급인데 **가동률이 5.6%**다. 더 지을 때가 아니라 돌아가게 만들 때다.

**결과**: 150 tests.

---

## 13회차 — 빈 찬장을 서른 개 지었다

**실측** (315분째, 12회차 배포 후 30분)

```
버너 인서터 30대 신설 (직전 0대)
  29대  waiting_for_source_items   ← 상자가 비었음
   1대  waiting_for_space_in_destination
돌 화로 27 → 44          (상한 해제가 먹히는 중)
랩: no_power 2 → no_power 1 + missing_science_packs 1   ← 하나에 전기가 붙었다
채굴기 no_fuel 66 → 70,  working 15 → 8
```

**좋은 것**: 전기망 잇기가 먹혔다. 랩 하나가 이제 전력을 받고 과학팩만
기다린다. 급유 장치도 30대가 실제로 세워졌다 — 기하는 맞았다.

**나쁜 것**: 서른 대 중 스물아홉이 «집을 게 없음»이다. `build_rig` 가 상자와
인서터를 **먼저** 놓고 석탄은 **나중에** `obtain` 했는데, 그게 실패하면
조용히 넘어가고 있었다. 빈 찬장을 서른 개 지어놓은 셈이다.

**판단**: 장치를 세우는 것과 밥을 넣는 것은 다른 일인데 하나로 묶어두고,
뒤쪽이 실패해도 앞쪽은 「성공」으로 남게 만들었다. **못 먹일 거면 짓지 않는
편이 낫다** — 안 지으면 다음에 다시 시도하지만, 빈 채로 지으면 그 자리는
「이미 장치가 있는 곳」이 되어 영영 안 고쳐진다.

그리고 순서가 하나 빠져 있었다. 빈 찬장 서른 개를 두고 서른한 번째를
지으러 가면 안 된다.

**조치**
- `build_rig()`: **석탄을 먼저 구한다.** 못 구하면 아무것도 짓지 않고 말한다.
- `hungry_rigs()` 리모트: `waiting_for_source_items` 인 인서터의 **집는 칸**을
  그대로 물어본다. 상자가 어디 있는지 다시 계산할 이유가 없다 — 인서터
  자신이 알고 있다.
- `restock_jobs()`: 빈 급유 상자에 석탄을 붓는다. 배차 순서를 셋으로 정리했다.

  ① 빈 급유 상자 채우기 → ② 새 급유 장치 세우기 → ③ 손으로 넣기

- 배차 경로는 `needs` 를 보지 않는다는 것을 확인하고, 실을 곳(`take`)을
  작업의 첫 단계로 직접 붙였다. 빈손으로 보내면 `insert` 가 그냥 실패한다.

**결과**: 150 tests.

---

## 14회차 — 나에게서 가까운 것과 일감에서 가까운 것

**실측** (344분째)

```
버너 인서터 37대 — 34대가 여전히 waiting_for_source_items
돌 화로 44 → 55,  no_ingredients 36 → 53
상자 속 석탄 3,402 → 9,958
채굴기 no_fuel 70 → 68,  working 8
```

석탄이 **9,958개** 쌓였는데 급유 상자는 여전히 비어 있다. 이번엔 짐작하지
않고 에이전트들이 무슨 말을 했는지 세어봤다:

```
 5  급유 상자 6개가 비었습니다. 석탄을 실어 붓겠습니다. (80, 74)   ← 배차는 됨
13  insert 실패: no coal to insert
 4  insert 실패: no path from 92.2,6.1 to -51,85                  ← 200타일
 3  take 실패: stuck at -58.1,-64.9 after 5 routes
 3  3명이 coal을 찾고 있습니다... coal 채굴기를 늘리겠습니다
```

**판단 1 — 창고를 「나에게서」 가까운 순으로 고르고 있었다.** `chest_stock`
은 부르는 사람 기준으로 정렬해 돌려준다. 그래서 (92,6)에 선 사람이 자기 옆
상자에서 석탄을 싣고 (−51,85)까지 걸어가라는 계획이 나왔다. 200타일이다.
길을 못 찾아 다섯 번 헤매다 빈손으로 「no coal to insert」로 끝난다.

싣는 곳은 **일감 옆**이어야 한다. 기준이 내가 아니라 일감이다.

**판단 2 — 석탄 9,958개를 두고 「석탄이 없습니다」라고 했다.** `shortage_jobs`
가 게시판의 부탁 수만 보고 판단했다. 손에 없는 것과 없는 것은 다르다. 이건
부족이 아니라 운반 문제인데, 대응이 「채굴기를 늘린다」였다. 9회차(반장),
11회차(계획 수립기)에 이어 **세 번째로 같은 눈먼 자리**다.

**조치**
- `nearest_to(spots, at, least, reach)`: 일감에서 가장 가까운 창고. `reach`
  밖이면 아예 없다고 답한다 — 못 할 일을 배차하는 것보다 낫다(120타일).
  급유 상자 채우기와 손 급유 둘 다 이걸 쓴다.
- `shortage_jobs()` 가 창고 재고를 먼저 본다. 부탁한 양보다 창고에 더 많으면
  「더 캘 일이 아니라 나를 일입니다」라고 말하고 부탁만 내린다.

**교훈 (세 번째)**: 「무엇이 있는가」를 묻는 자리마다 창고를 빠뜨렸다.
반장, 계획 수립기, 부족 판단. 다음에 같은 질문을 하는 코드를 쓰면 **창고를
보는지부터** 확인할 것.

**결과**: 155 tests.

---

## 15회차 — 호수 건너에 한 명이 갇혀 있었다

**실측** (372분째). 처음으로 **가동률이 올라갔다**:

```
채굴기 no_fuel     68 → 52
채굴기 working      8 → 18
돌 화로            55 → 56,  no_ingredients 53 → 54
상자 속 석탄    9,958 → 12,186
```

그런데 급유 상자 34개는 여전히 비어 있고, 실패의 모양이 바뀌었다:

```
9  급유 상자 3개가 비었습니다. 석탄을 실어 붓겠습니다. (104, 78)
5  take 실패:   no path from 92.2,6.1 to 93,79
5  insert 실패: no path from 92.2,6.1 to 90,76
5  insert 실패: no path from 92.2,6.1 to 88,79
5  insert 실패: no path from 92.2,6.1 to 83,79
```

**같은 자리에서 다섯 번씩, 네 목적지 모두.** 지형을 1타일 단위로 찍어봤다:

```
x=80..104, y=-8..16   (~ = 물)
   5 ~~~~~~~~~~~~.............
   6 ~~~~~~~~~~~~.............   ← 에이전트가 여기(92.2, 6.1)
  12 ~~~~~~~~~..~~~...........   ← x=92 가 여기서 물로 막힌다
  13 ~~~~~~~~~~~~~~~..........
```

**호숫가에 갇혀 있었다.** x=92 열은 북쪽으로만 뚫려 있고 y≥12부터 물이다.
목적지는 남쪽 y=79 — 직선거리 73타일(HAUL_REACH 120 안)이지만 걸어서는
물을 크게 돌아야 한다. **유클리드 거리는 물이 있으면 거짓말을 한다.**

**판단 — 진짜 문제는 배차에 있었다.** 코드를 열어보니:

```python
name = order[0]                      # 가장 가까운 한 사람만 본다
if job.key in worker.blocked_now():
    continue                         # 그 사람이 막아뒀으면 일감을 버린다
```

가장 가까운 사람이 이 일을 막아뒀으면 **일감 자체가 사라졌다.** 호수 건너
한 명 때문에 급유 상자 채우기가 통째로 없어지고 있었다 — 다른 셋은 갈 수
있는데도. 그리고 배차는 유클리드 거리로 「가장 가까운 사람」을 고르므로,
물 건너편 사람이 언제나 1순위였다.

**조치**
- 배차가 후보 목록을 **끝까지 걸어 내려간다.** 한 사람이 못 하는 일을
  통째로 버리지 않는다.
- 「길이 없다」/「stuck」으로 실패한 일감은 **그 사람에게만** 15분 막아둔다.
  세상에 길이 없다는 뜻이 아니라 이 사람이 못 간다는 뜻이다. 호수는 다음
  배차 때도 그 자리에 있다.

**결과**: 155 tests.

---

## 16회차 — 비우기는 스무 분짜리 해결이다

**실측** (401분째)

```
채굴기 waiting_for_space_in_destination 109 → 112
채굴기 no_fuel 52 → 58,  working 18 → 9
돌 화로 no_ingredients 54
급유 상자 34개 여전히 빔
상자 속 석탄 12,186 → 17,157   (계속 쌓이기만 한다)
```

발언에서:

```
8  급유 상자 6개가 비었습니다. 석탄을 실어 붓겠습니다. (94, 78)
9  insert 실패: no coal to insert
```

**판단 1 — 실을 양과 부을 양이 안 맞았다.** 급유 상자 여섯 개짜리 묶음이면
300개가 필요한데, 그만한 상자가 없으면 50개짜리로 떨어지고 `load` 는
`min(source, wanted)` 라 60개만 싣는다. 첫 상자에서 50을 쓰고 나머지 다섯
번이 「no coal to insert」로 끝난다. 아홉 번 그랬다.

→ 실을 수 있는 만큼으로 **묶음을 줄인다.** 여섯 개를 채우겠다고 나서놓고
넷만 채우느니, 처음부터 하나만 채우겠다고 하는 편이 낫다.

**판단 2 — 「비우기」는 이십 분짜리 해결이다.** 채굴기 112대가 꽉 찬 상자에
막혀 선 옆에서 화로 54대가 굶고 있다. 둘은 **같은 문제의 두 얼굴**이다.
그런데 우리 대응은 「상자를 비운다」였고, 비우면 이십 분 뒤에 똑같이 막힌다.
네 명이 112대를 계속 비우는 경주는 이길 수 없다.

광석이 든 상자라면, 비우는 대신 **그 자리를 화로로 바꾼다:**

```
[채굴기] → [꽉 찬 상자]     캐낸 광석이 갈 데가 없다
[채굴기] → [화로]           캐는 대로 바로 녹는다. 나를 일이 없어진다.
```

상자를 캐면 안에 든 것이 가방으로 따라오니, 그 광석을 그대로 새 화로에
넣으면 버리는 것도 없다. 채굴기 하나가 살아나고 화로 하나가 늘고 운반
일감 하나가 사라진다 — 한 번의 걸음으로 셋이다.

**조치**
- `restock_jobs()`: 실을 수 있는 만큼으로 묶음을 줄인다.
- `convert_chest()`: 녹일 수 있는 광석이 찬 상자는 걷어내고 화로를 세운다.
  `demolish` 가 6회차에 생겨 있어서 그대로 쓸 수 있었다.

**결과**: 155 tests.

---

## 17회차 — 한 명이 한 시간 넘게 호숫가에 서 있었다

**실측** (429분째). 아무것도 안 늘었다:

```
화로 56 → 56    인서터 37 → 37    상자 150 → 150
철판 525 → 525  철광석 9,152 → 9,152
채굴기 working 9 → 5,  막힌 채굴기 112 → 119
```

**변한 것은 석탄(+1,920)과 돌(+868)뿐.** 짓는 일이 하나도 안 되고 있었다.
그런데 「급유 장치를 세우겠습니다」는 열두 번 나왔다.

먼저 계획 수립기를 의심해 직접 물어봤다 — 멀쩡했다:

```
burner-inserter x1  ready=true  fetch=[iron-plate 1 @(-59.5,77.5)]  steps=[craft]
stone-furnace  x1  ready=true
coal          x50  ready=true  fetch=[coal 45 @(79.5,73.5)]
```

**판단 1 — 배차가 자기 자신에게 거짓말을 하고 있었다.**

```python
if job.routine:
    self.start_routine(worker, job.routine, job.ore, job.at)   # 반환값을 버린다
handed.add(name)
```

`start_routine` 은 앞의 긴 작업이 아직 슬롯을 쥐고 있으면 아무것도 안 하고
`False` 를 돌려준다. 그런데 배차는 그걸 성공으로 치고 narration 을 말한 뒤
일감을 점유했다. **열두 번 말하고 한 대도 안 세운** 이유다.

→ 「시작할 수 있는가」를 먼저 확인하고 **그 다음에** 말한다. 시작하지 못한
일을 시작했다고 치지 않는다.

**판단 2 — 한 명이 갇혀 있었다.** 발언 200줄 중 서로 다른 것이 44종인데,
그중 **25종이 한 좌표에서 나왔다:**

```
insert 실패: no path from 92.2,6.1 to -54,-66
insert 실패: no path from 92.2,6.1 to -81,4
insert 실패: no path from 92.2,6.1 to -63,-59
...  목적지는 전부 다르고, 출발지는 언제나 (92.2, 6.1)
```

15회차에 「그 사람에게만 일감을 막는다」로 고쳤는데 소용이 없었다. **목적지가
매번 다르니 일감 키도 매번 새것**이라, 막아도 다음 새 일감이 또 온다.
막아야 할 것은 일감이 아니라 그 사람의 처지였다.

여덟 중 하나가 한 시간 넘게 죽은 인력이었을 뿐 아니라, 배차가 유클리드
거리로 고르니 **동쪽 일감에 「가장 가까운 사람」이라 일감을 계속 빨아들였다.**

**조치**
- `where_lost()`: 실패 메시지에서 **앞쪽** 좌표를 읽는다. 뒤는 목적지라
  매번 다르고, 갇힌 것은 앞자리다. 순수 함수라 게임 없이 검증한다.
- 같은 자리에서 세 번 연달아 못 가면 `unstick()` — 동료 옆으로 옮긴다.
  한 번은 운이 나쁜 것이고 세 번은 지형이다. 걸어서 못 나오는 곳에 있으면
  걸어서 꺼낼 수 없다.

**결과**: 160 tests.

---

## 18회차 — 짓는 일이 살아났다

**실측** (458분째). 17회차 배포 후 처음으로 **건물이 늘었다**:

```
버너 인서터  37 → 48   (+11)
철 상자     150 → 161  (+11)   ← 급유 장치가 실제로 세워지고 있다
채굴기 no_fuel  55 → 44
「no path from 92.2,6.1」  사라짐   ← 갇힌 사람이 풀려났다
```

그리고 가장 좋은 신호:

```
인서터 waiting_for_space_in_destination  1 → 14
```

이건 **기계의 연료 슬롯이 꽉 차서 인서터가 쉬는 상태**다. 급유 장치가
제 일을 하고 있다는 뜻이고, 그 열네 대는 이제 사람 손이 필요 없다.

**아직 안 된 것**

```
돌 화로 56 → 56                          ← 상자→화로 전환이 0번
채굴기 막힘 119 → 123,  마른 채굴기 16 → 20
화로 no_ingredients 54
```

**판단 — 무더기로 있는 일을 개별 경로에만 달아뒀다.** 16회차에 만든
`convert_chest` 를 `tend_job`(한 사람이 할 일을 하나 고르는 자리)에만
연결했는데, 거기엔 「정비는 절반만」 같은 제동이 걸려 있어 스물아홉 분
동안 한 번도 안 불렸다. **123대짜리 무더기는 배차에 올려야 한다.**

**조치**
- 상자→화로 전환을 `survey()` 의 3단계로 올렸다. 한 번에 넷씩.
- 마른 채굴기(`no_minable_resources`)를 `FIXABLE` 에 `spent` 로 추가하고,
  걷어내는 일감을 3b단계로 넣었다. 「고장」이 아니라 「끝난 것」이라 손볼
  방법이 없고, 걷어내면 채굴기가 통째로 재고로 돌아온다. 5회차에 매장량으로
  자리를 고르게 만든 것이 여기서 쓸모가 있다 — 다시 세울 때 두꺼운 곳에
  세운다.

**결과**: 160 tests.

---

## 19회차 — 막힌 123대는 한 가지 문제가 아니었다

**실측** (485분째)

```
채굴기 195 → 171   ← 마른 채굴기 걷어내기가 먹혔다 (no_minable_resources 사라짐)
인서터  48 → 55,   처음으로 working 2
상자   161 → 168
돌 화로 56 → 56    ← 상자→화로 전환은 여전히 0번
막힌 채굴기 123 그대로
```

18회차에 전환을 배차로 올렸는데도 한 번도 안 일어났다. 그래서 **막힌 123대가
정확히 무엇에 막혔는지** 세어봤다:

```
받을 곳이 아예 없음            83   ← 땅에 떨구다 막힘
상자가 돌로 꽉 참              22   ← 창고에 이미 48,099개
상자가 석탄으로 꽉 참            6   ← 창고에 이미 19,590개
석탄 드릴끼리 서로 먹임          10   ← 정상(자가급유 쌍)
상자가 철광석으로 꽉 참           2
```

**판단 — 「상자가 있고, 녹일 수 있는 광석」이라는 조건이 2대에만 해당했다.**
123대짜리 무더기를 하나의 문제로 보고 하나의 손을 준비했는데, 실제로는 셋이
섞여 있었고 그중 제일 큰 것(83대)은 조건에 아예 안 걸려 통째로 건너뛰어졌다.

그리고 **돌이 48,099개 있는데 22대가 여전히 돌을 캐고 있었다.** 캐는 것도
일이고, 막힌 채로 서 있는 것도 자리다. 「그만 캐도 된다」는 판단이 코드에
아예 없었다.

**조치** — 셋으로 갈라 각각 맞는 손을 쓴다.

| 상태 | 손 |
|------|-----|
| 창고에 6,000개 넘게 쌓인 것을 캐는 중 | 걷어낸다 (`enough:`) |
| 녹일 수 있는 광석이 찬 상자 | 화로로 바꾼다 (`convert:`) |
| 받을 곳이 아예 없음 | 받을 것을 단다 (`outlet:`) |

`SURPLUS = 6000`. 이건 「무엇을 지을까」가 아니라 **「무엇을 그만둘까」**를
정하는 첫 규칙이다. 지금까지 이 방향의 판단은 하나도 없었다.

**결과**: 160 tests.

---

## 20회차 — 약속과 짐은 같은 저울에 올려야 한다

**실측** (513분째). 셋으로 가른 것이 먹히기 시작했다:

```
막힌 채굴기 123 → 109
채굴기      171 → 155   ← 돌 채굴기 걷어내기(enough:)가 작동
인서터       55 → 58,  상자 168 → 171
```

남은 실패 두 가지가 선명했다:

```
8  insert 실패: no coal to insert
7  insert 작업이 막혔습니다 — coal 25개가 필요합니다. 여유 있는 분 부탁드립니다.
```

**판단 1 — 16회차에 고친 버그가 옆방에 그대로 있었다.** 급유 상자 채우기에서
「실을 수 있는 만큼으로 묶음을 줄인다」로 고쳤는데, **손 급유 경로에는 같은
실수가 남아 있었다.** 채굴기 여섯 대에 25개씩 넣겠다(=150개)고 나서놓고
`min(HAUL_BATCH=40, …)` 로 40개만 싣고 갔다. 두 대 넣고 끝난다.

두 번 같은 실수를 했으니 이름을 붙여 순수 함수로 뺐다:

```python
def carry_split(group, load, each) -> (group, load):
    """여섯 대에 넣겠다고 말하고 둘만 넣느니,
       처음부터 둘에게 넣겠다고 말하는 편이 낫다."""
```

세 군데(급유 상자·손 급유·화로 먹이기)가 이제 같은 저울을 쓴다. 게임 없이
검증되므로 세 번째는 없다.

**판단 2 — 화로 54대가 다섯 회차째 굶고 있었다.** 그 사이 철광석은 창고에
10,264개까지 쌓였다. 캐는 능력이 모자란 적은 한 번도 없고, **캔 것이
화로까지 가지 않을 뿐이다.** 그런데 「굶는 화로에 광석을 실어다 준다」는
일감이 배차에 아예 없었다(개별 경로에만 있었고, 거기엔 제동이 걸려 있다).

18회차의 교훈이 그대로 반복됐다 — **무더기로 있는 일은 배차에 올려야 한다.**

**조치**
- `carry_split()`: 약속과 짐을 같은 저울에. 세 군데 통일.
- 3c단계: 굶는 화로를 구역 단위로 먹인다. 상자는 **화로 옆에서** 고른다.

**결과**: 166 tests.

---

## 21회차 — 과학팩을 주머니에 넣고 아홉 시간

**실측** (539분째)

```
막힌 채굴기 109 그대로,  마른 채굴기 1 (거의 정리됨)
화로 먹이기 배차 성공:  「화로 2대가 굶고 있습니다. iron-ore 40개 실어다 넣겠습니다」
그런데              15  insert 실패: no iron-ore to insert
```

가방이 꽉 찬 건가 싶어 에이전트 넷의 소지품을 열어봤다. 꽉 차지 않았는데,
대신 다른 것이 보였다:

```
alpha    burner-mining-drill 9, iron-plate 300, stone-furnace 2 …
bravo    steam-engine 2, iron-plate 162, automation-science-pack 10   ←
charlie  lab 1, iron-plate 309, copper-plate 59, automation-science-pack 10   ←
delta    boiler 1, steam-engine 2, stone 107 …
```

**빨간 과학팩 20개를 주머니에 넣고 다니고 있었다.** 그리고 전기가 들어온
랩은 `missing_science_packs` 상태로 서 있었다.

**아홉 시간 동안 연구가 멈춰 있던 이유가 이것이다 — 만들어 놓고 넣지를
않았다.** 사다리에는 「빨간 과학팩 10개를 갖는다」까지만 있었고, 그 다음
한 걸음인 「랩에 넣는다」가 어디에도 없었다. 갖는 것과 쓰는 것은 다르다.

**조치**
- `missing_science_packs` 를 `FIXABLE` 에 `science` 로 추가. 이건 사람 손으로
  고칠 수 있는 종류다.
- 배차 최우선(`jobs`, 인터리브 앞)에 랩 채우기를 넣었다. 사다리 전체의
  목이라 다른 무엇보다 앞이다 — 연구가 돌면 automation 이 열리고 조립기와
  롱암 인서터가 열린다.
- `Job.owner`: **가진 사람이 넣는다.** 배차는 가장 가까운 사람을 고르는데,
  주머니에 든 것을 넣는 일은 가까운 사람이 아니라 가진 사람의 일이다.
  가방을 부리는 일(`depot:`)만 쓰던 주인 지정을 일반화했다.
- 소지자는 이번 틱 스냅샷에서 찾는다. `self.stock` 은 사람이 지시를 내릴
  때만 채워지므로 자동으로 도는 동안에는 비어 있다.

**결과**: 166 tests.

---

## 22회차 — 연구가 뚫렸다

**실측** (562분째, 21회차 배포 후 20분)

```
TECHS 3 → 6
  automation ✓   조립기 1호, 롱암 인서터
  military ✓     건 터렛, 총알
  logistics ✓    지하 벨트, 분배기
  진행 중: electric-mining-drill
```

**과학팩을 랩에 넣은 것 하나가, 아홉 시간 막혀 있던 사다리를 스무 분 만에
세 칸 밀어올렸다.** 스물한 회차 동안 고친 것 중 가장 값싸고 가장 크다.

**판단 — 그 스무 개는 누가 손으로 만든 것이다.** 다 쓰면 다시 멈춘다.
이번에 뚫린 `automation` 이 조립기를 열어줬으니, 이제 랩이 스스로 먹게
할 수 있다:

```
[조립기] → [인서터] → [랩]
```

조립기 1호가 만드는 과학팩 0.1개/초 = 랩 한 대가 먹는 0.1개/초.
**정확히 한 대가 한 대를 채운다.** 초반에 이보다 깔끔한 비율은 없다.

**조치**
- `set_recipe()`: 조립기에 무엇을 만들지 정해준다. **이 기능이 아예 없어서**
  조립기를 세울 수가 없었다. 레시피 없는 조립기는 전기만 먹는다.
- `assembler_site()`: 자리는 계산하지 않고 세워보고 묻는다. 인서터가 실제로
  조립기에서 집어 랩에 넣는지는 `pickup_position` 과 `drop_position` 이
  답해준다 — 인서터 방향을 실측으로 알아낸 12회차와 같은 방법이다.
- `build_science()`: 조립기와 인서터를 세우고, 레시피를 넣고, 전봇대가 닿는
  자리를 물어 전기를 대고, 첫 재료를 넣는다.
- 랩 채우기 일감과 **함께** 낸다. 넣는 일과 「다시는 안 멈추게 하는 일」은
  다른 일이다.

**결과**: 166 tests.

---

## 23회차 — 없을 때 포기하면 영원히 없다

**실측** (591분째)

```
조립기 0대           ← 한 대도 안 섰다
RESEARCH electric-mining-drill 0%,  TECHS 6 그대로
lab missing_science_packs 1        ← 손으로 만든 팩 스무 개를 다 썼다
10  insert 실패: no coal to insert      (창고에 석탄 22,884개)
 9  insert 실패: no iron-ore to insert
```

22회차에 조립기 짓는 일감을 넣었는데 한 번도 안 나갔다. 코드를 다시 읽었다:

```python
if not holder:
    source = nearest_to(shelves, at, 1)
    if not source:
        continue        # ← 조립기 짓는 일감까지 같이 건너뛴다
...
if not snap_all.get("assembling-machine-1"):
    jobs.append(조립기 세우기)
```

**과학팩이 하나도 없으면 `continue` 로 빠져나가는데, 그 밑에 「과학팩을
만드는 기계를 세운다」가 있었다.** 팩이 없을 때 특히 해야 하는 일이,
팩이 없다는 이유로 건너뛰어졌다.

없을 때 포기하면 영원히 없다. 조립기를 **먼저** 낸다.

**판단 2 — 창고에 석탄이 22,884개인데 「no coal to insert」가 열 번.**
`nearest_to` 가 「양이 되는 것 중 가장 가까운 것」을 고르는데, 딱 맞는 양만
든 상자는 우리가 도착할 때쯤 이미 비어 있다. 여덟이 같은 상자를 노리고
있어서다.

→ **넉넉한 곳을 먼저 본다.** 필요한 양의 네 배가 든 상자를 우선하고, 없을
때만 딱 맞는 것으로 내려간다. 조금 더 걷더라도 도착해서 빈손인 것보다 낫다.

**결과**: 168 tests.

---

## 24회차 — 전력이 처음으로 0이 아니었다

**실측** (618분째)

```
assembling-machine-1 = 1   ← 조립기가 섰다
inserter = 1
POWER 2900 W               ← 이번 세션 통틀어 전력이 0이 아닌 첫 순간
```

열 시간 동안 0W 였던 것이, 조립기와 인서터가 전기를 «쓰기» 시작하자 흐르기
시작했다. 발전기는 계속 돌 준비가 되어 있었고, 쓰는 사람이 없었을 뿐이다.

**남은 것**: 조립기 상태가 `item_ingredient_shortage` — 레시피는 들어갔는데
재료가 없다. 조립기는 구리판과 철기어를 먹는데, 지금 있는 「먹이기」 일감은
화로용 광석(`SMELTED_BY_FURNACE`)만 안다.

**판단** — 무엇이 몇 개 모자란지 **짐작할 이유가 없다.** 레시피의 재료에서
입력 칸에 이미 든 것을 빼면 그것이 곧 가져다줄 목록이고, 그 계산은 게임이
한다. 지금까지 배관도 전봇대도 인서터 방향도 전부 이 방법으로 풀었다.

**조치**
- `item_ingredient_shortage` → `supply`. `broken()` 이 `wants` 에
  「무엇을 몇 개」를 담아 돌려준다(열 번 돌릴 분량).
- 공급 일감: 창고에 있으면 실어 가고, **없으면 만들어서 간다.** 철기어는
  아무도 상자에 넣어두지 않지만 철판으로 언제든 만들 수 있다 — 「없다」와
  「못 만든다」는 다르다.

**결과**: 168 tests.

---

## 25회차 — 사슬이 돌기 시작했다

**실측** (645분째)

```
RESEARCH electric-mining-drill  0% → 48%     ← 연구가 다시 돈다
POWER 2900 W 유지
조립기 공급 일감 배차됨: 「조립기가 재료를 기다립니다. copper-plate 10개를 대겠습니다」
```

열한 시간 만에 사슬이 처음으로 제 힘으로 돌기 시작했다:

```
화로 → 판금 → 조립기 → 과학팩 → 랩 → 연구
```

**남은 최다 실패 — 빈손 도착.** 「no coal to insert」 7회,
「no iron-ore to insert」 10회. 창고에는 석탄 22,884개, 철광석 9,452개가
있는데도.

23회차에 「넉넉한 상자를 먼저 고른다」로 고쳤는데도 남았다. 그래서 `take`
자체를 열어봤다:

```lua
local taken = st.target.remove_item { ... }
if taken > 0 then bot.insert { ... } end
ctx.task.result = { taken = taken, ... }
return "done"          -- ← 0개를 가져와도 «done»
```

**한 개도 못 가져왔는데 성공이었다.** 그래서 크루는 빈 상자까지 걸어가
성공으로 치고, 뒤따르는 insert 가 하나씩 무너졌다. **실패한 자리는 insert
였지만 틀린 자리는 take 였다.**

여덟이 같은 상자를 노리므로, 계획을 세울 때 들어 있던 것이 도착할 때
남아 있으리라는 보장이 없다. 그 사실을 `take` 가 말해줘야 부르는 쪽이 그
상자를 잠시 접어둘 수 있다.

**조치**: `take` 가 0개면 실패한다. 「비어 있다」고 말한다.

**결과**: 168 tests.

---

## 26회차 — 공장이 스스로 돈다

**실측** (672분째)

```
assembling-machine-1 working        ← 조립기가 과학팩을 만들고 있다
POWER 2,900 → 77,900 W              ← 27배
TECHS 6 → 7                          electric-mining-drill 완료
RESEARCH steel-processing 95%        ← 연구가 끊이지 않는다
```

**열한 시간 만에 사슬 전체가 사람 손 없이 돈다:**

```
화로 → 판금 → 조립기 → 과학팩 → 인서터 → 랩 → 연구 → 다음 연구
```

전력이 27배가 된 것은 발전기를 더 지어서가 아니다. **쓰는 기계가 생겼기
때문이다.** 발전기는 열한 시간 내내 돌 준비가 되어 있었다.

**25회차의 수정이 진짜 원인을 드러냈다.** `take` 가 빈손을 실패로 말하기
시작하자, 가려져 있던 것이 보였다:

```
10  take 실패: burner-inserter is empty of coal
```

**석탄 상자에서 꺼내려는데 한 칸 옆에 붙은 급유 인서터를 집고 있었다.**
`target_entity` 의 반경이 1.5라 둘 다 잡히고, 먼저 나오는 쪽이 이겼다.
급유 장치가 상자와 인서터를 한 칸 간격으로 세우므로 이 둘은 **언제나**
같이 잡힌다 — 12회차에 내가 만든 배치가 이 버그를 매번 재현하고 있었다.

인서터도 연료 칸이 있어 `get_inventory` 를 통과한다. 「창고인가」로 거르는
것보다 **「요청한 자리인가, 그리고 가지고 있는가」**로 고르는 편이 옳다 —
화로에서 거두는 일도 같은 함수를 쓰기 때문이다.

**교훈**: 실패를 성공이라 말하는 코드는 버그를 하나 만드는 게 아니라
**버그를 보이지 않게 만든다.** 열 번의 「no coal to insert」 뒤에 숨어 있던
것은 전혀 다른 문제였다.

**결과**: 168 tests.

---

## 27회차 — 허공을 집는 인서터, 그리고 바이터

**실측** (699분째). 연료 문제가 거의 풀렸다:

```
채굴기 no_fuel 45 → 6,   working 0 → 36
화로 56 → 94 (working 4)
TECHS 7 → 9  (steel-processing 완료, advanced-material-processing 시작)
transport-belt = 3     ← 벨트가 처음 등장
사다리 9/12단계 — 초록 과학팩
```

26회차에 고친 「엉뚱한 대상을 집던 문제」가 연료 공급을 통째로 열어줬다.

**문제 1 — 조립기는 꽉 찼는데 인서터가 허공을 집는다.**

```
assembling-machine-1 full_output
inserter waiting_for_source_items
```

좌표를 찍어봤다:

```
assembler  (46.5, -13.5)  bbox y -14.7 .. -12.3
inserter   (46.5, -11.5)  pickup (46.5, -12.5)
what is at the pickup tile: NOTHING
assembling_machine_output: automation-science-pack = 10
```

**3×3 엔티티의 충돌 상자는 타일 발자국보다 0.3씩 안쪽이다.** 집는 칸이
조립기에 0.2타일만 걸쳤고, 게임은 그걸 「닿지 않음」으로 봤다. 과학팩 열
개를 만들어놓고 인서터가 허공을 집고 있었다.

→ 좌표를 믿지 않는다. 임시로 **조립기와 인서터를 둘 다 세워보고**
`pickup_target` 과 `drop_target` 을 인서터에게 직접 묻는다. 그보다 확실한
근거는 없다. 거리도 3~5칸을 훑는다.

**문제 2 — 바이터가 왔다** (사용자 보고, 실측으로 확인)

```
적 유닛 12 (전부 small-biter)
가장 가까운 둥지 (-43,-229) 232타일
파손된 건물 0     터렛 0
gun-turret 레시피 = false
```

`military` 는 22회차에 끝났는데 터렛이 잠겨 있었다. 프로토타입을 뒤져보니:

```
gun-turret  -> gun-turret      done=false  cost=10 (빨간팩)
military    -> submachine-gun  done=true      ← 이건 기관단총만 연다
stone-wall  -> stone-wall      done=false  cost=10
```

**「military」를 방어라고 알고 있었다.** 터렛은 `gun-turret` 이라는 별개의
연구고 벽도 `stone-wall` 로 따로다. 이름이 그럴듯해서 확인하지 않았고,
그 연구가 끝난 뒤에도 터렛은 잠긴 채였다 — 그리고 바로 그때 적이 왔다.

→ `RESEARCH_ORDER` 를 `gun-turret`, `stone-wall` 우선으로. 적이 이미 와
있는데 터렛이 잠겨 있으면 **하던 연구를 끊고** 터렛부터 뚫는다.

두 문제는 한 줄로 이어진다: **조립기 출력에 갇힌 과학팩 열 개가 곧
gun-turret 연구 비용 열 개다.** 인서터를 고치는 것이 곧 방어를 여는 것이다.

**결과**: 168 tests.

---

## 28회차 — 새 판, 그리고 「고쳤다」가 절반만 고쳐진 것들

지난 판(700분, 채굴기 195대 중 100대 막힘, 길도 벽도 없음)을 접고 새 맵으로
시작했다. 27회차분 개선이 처음부터 적용되는 대신, **자원이 0인 상태**가
지난 판에서 가려져 있던 것들을 전부 드러냈다.

### 하루에 잡은 다섯 가지 — 전부 같은 모양

| 증상 (사용자 보고) | 실제 원인 |
|---|---|
| 관찰자 명령이 안 먹힘 | 반장이 **18회차 동안 `NameError` 로 죽어 있었음** |
| 애들이 구리만 캠 | `mine` 이 「무엇을」 캐라는 걸 무시하고 겹친 구리를 캠 |
| 또 채굴기 끝자락에 | 후보를 테두리부터 40개만 모은 뒤 정렬 — 정렬 대상이 틀림 |
| 석탄 자급쌍이 왜 안 됨 | `drill_site` 가 광석을 안 가려 딴 일감이 석탄에 상자를 닮 |
| 공용 상자를 이해 못 함 | 반장 명령 목록에 `depot` 이 **없음** |

**기능은 있는데 그것을 가리키는 것이 없거나 틀렸다.** 다섯 다 그 모양이다.

### 가장 뼈아픈 것 — 반장이 18회차 동안 죽어 있었다

```
NameError: name '_stores_text' is not defined
```

9회차에 「반장에게 창고를 보여주기」를 넣을 때, 프롬프트의 `[창고]` 줄은
들어갔는데 함수 정의가 파일에 들어가지 않았다. 그 자리의 편집이
AssertionError 로 실패했는데, **프롬프트 줄만 따로 다시 넣고 함수는 잊었다.**

`import` 는 통과한다. 파이썬은 그 이름을 **호출할 때야** 찾기 때문이다.
그래서 176개 테스트가 전부 녹색인 채로, 사람이 채팅에 쓴 모든 지시가
NameError 로 죽었다. 예외는 스레드 안에서 잡혀 stderr 로만 나갔다.

→ 게임도 모델도 없이 **프롬프트 조립까지 가보는** 테스트를 넣었다. 없는
CLI 를 주면 `None` 이 돌아와야 하고, 그 경로가 NameError 를 만나면 실패한다.

### 세 번은 「고쳤다」가 절반만 고쳐진 것이었다

- **5회차 매장량**: 자리를 매기게는 했는데, 매길 대상을 테두리에서 모았다.
  정렬은 했지만 정렬할 목록이 이미 틀려 있었다. → 광맥 전체를 받아
  **고르기 전에** 줄을 세운다.
- **12회차 자급쌍**: `automate_coal` 은 맞았는데, `drill_site` 가 광석을
  안 가려서 다른 일감이 석탄 위에 상자 달린 채굴기를 세웠다. 쌍이 될
  자리를 먼저 뺏은 것이다.
- **9회차 창고 가시성**: 위의 NameError.

### 결과 (34분째)

```
새 채굴기 밑의 매장량:  철 3,522 / 돌 3,869 / 구리 5,471
예전 것(수정 전):       석탄 641, 540, 120, 162
TECHS 3 @ 34분          (지난 판은 280분 — 8배)
research = gun-turret    (방어 우선 순서가 먹힘)
```

### 이번 회차에 더한 것

`poor_drills()` — 마르기를 **기다리지 않는다.** 2×2에 400개도 안 남은
채굴기는 걷어내서 두꺼운 자리에 다시 세운다. 400이면 27분이고 옮기는 데
드는 것은 한 번의 걸음인데, 같은 광맥의 두꺼운 칸은 보통 2,000이 넘는다.
완전히 마를 때까지 기다리는 것은 몇 분 더 캐려고 이십 분을 버리는 일이다.

**결과**: 176 tests.

---

## 29회차 — 한 파일 4,080줄을 가르고, 자급쌍을 창고로 본다

**지시** (사용자). 두 가지가 같은 날 들어왔다.

> "스크립트 하나하나에 기능 다 떄려박아서 코딩하는거같은데, 여러 스크립트로
> 기준에맞게 나눠서 우아한 프로그래밍을 할 것. 이건 최우선적으로 기본적으로
> 지켜야함"

> "석탄 채굴기 이제 서로 마주보는 구조물이 생겼는데, 얘네는 출구가 서로
> 잡혀있다보니 추출물들이 연료로 서로 들어가는구조임. 어짜피 석탄을 캐는
> 석탄채굴기니까 얘네구조물은 연료칸에서 몇개씩 빼서 가져가는 기능으로 생각해"

**실측.** `agent.py` 4,080줄, 그중 `Crew` 클래스 하나가 3,000줄.

**판단 1 — 파일을 가르는 기준은 줄 수가 아니라 책임이다.** 이번 세션에만
「기능은 있는데 그것을 가리키는 것이 없거나 틀렸다」를 세 번 겪었다(5회차 광량
정렬, 12회차 석탄쌍, 9회차 창고 가시성). 세 번 다 «그 기능이 어디 있는지 아무도
모르는» 파일에서 벌어졌다. 새 기능을 넣을 자리가 파일 이름만 보고 정해지지
않으면, 이미 있는 기능을 못 찾고 또 만든다.

**조치 1.** 17개 모듈로 갈랐다. 가장 큰 파일 470줄.

```
순수 계산   layout.py(어디에 놓나)  ladder.py(무엇을 하나)
값 객체     world.py(Snapshot)  jobs.py(Job)  worker.py(Worker)
숫자        settings.py
게임 접촉   crew/{roster,talk,supply,power,mining,factory,survey,tending,watch}
진입점      agent.py 105줄 - 전부 다시 내보낸다
```

손으로 옮기면 빠뜨리므로 구문 트리에서 잘라 옮겼고, **그래도 빠뜨렸다** —
`Crew.CREW_COMMANDS` 는 함수가 아니라서 딸려오지 않았다. 테스트가 잡았다.
옮긴 뒤 원본과 대조해 메서드 96개·최상위 이름 86개 전부 있는 것을 확인했다.

**판단 2 — 자급쌍은 구조물이 아니라 창고다.** 마주보는 두 대는 캔 석탄을
서로의 연료칸에 넣는다. 출력 상자가 없으니 캔 석탄이 갈 곳은 연료칸뿐이고,
연료칸은 한 칸(50개)이다. 즉 이 쌍은 **스스로 채워지는 석탄 창고**이고,
가득 차면 스스로 멈춘다. 덜어 가는 것이 사용법이다.

숫자로 확인: 버너 채굴기는 초당 0.25개를 캐고 0.0375개를 태운다(150kW ÷
4MJ). 캐는 것이 태우는 것의 6.7배다. 25개만 남겨두면 다시 가득 찰 때까지
멈추지 않는다.

**조치 2.** 새 경로를 만들지 않았다. 「석탄 어디 있나」를 묻는 자리가 이미
하나 있다(`chest_stock`). 거기가 자급쌍의 연료칸도 보게 했다:

```lua
if item == "coal" then
  for _, well in pairs(coal_banks(...)) do out[#out + 1] = well end
end
```

이러면 급유 상자 보충도, 손으로 캐는 대신 꺼내오는 것도, 게시판 요청도 전부
그대로 자급쌍을 쓴다. 철광석 위에 선 채굴기는 제외한다 — 그쪽 연료를 빼 가는
것은 창고에서 꺼내는 게 아니라 그 기계를 세우는 일이다.

**결과.** 배포 후 실측 예정.

---

## 30회차 — 바닥의 광석이 제 출구를 막고 있었다

**실측** (살아있는 판, 배포 직후).

```
burner-mining-drill  92대 중 24대만 돎, 68대 waiting_for_space_in_destination
stone-furnace        52대 중  6대만 돎, 46대 no_ingredients
transport-belt        0개
석탄 위 채굴기       48대, 전부 연료칸 50/50 으로 꽉 참
기지 무게중심        (81, 27)   요원들: (140,-12) (147,24) (162,36)
기지 60타일 밖 화로  43대 (가장 먼 것 242타일)
```

**판단 1 — 아무도 기지를 보고 있지 않았다.** 에이전트 중심 조회는 전부 그
사람 반경 200 안만 본다. 요원이 (322,13)에 서 있으면 기지가 «안 보이고», 안
보이니 할 일이 없고, 할 일이 없으니 돌아올 이유도 못 찾는다. 화로가 동쪽으로
줄줄이 도망가던 시절이 사람까지 그쪽으로 끌고 간 것이다.

**조치 1.** 반경에 매이지 않는 눈을 하나 만들었다 — `base()` 는 우리 건물
전체의 무게중심을 돌려준다. 순찰 맨 앞에서 그것과 자기 위치를 견주고,
120타일 밖이면 할 일을 찾기 전에 돌아온다. 그리고 `strays()` 로 도망간 화로를
걷어오게 했다. 걷어내면 화로가 통째로 손에 돌아오므로, 바로 다음 항목(눈먼
채굴기)에 쓸 수 있다. 화로 43대와 눈먼 채굴기 21대는 그대로 짝이 맞는다.

**결과 1.** 도망 화로 43 → 33대, 요원들이 x=322 에서 x=140~160 으로 복귀.

**판단 2 — 「자리가 없다」가 아니라 「치울 일이 있다」였다.** 눈먼 채굴기
앞에 화로를 놓으려 했더니 후보 자리 여든네 곳이 **전부** 거절당했다. 무엇이
막고 있는지 물어보니 `item-on-ground` — 그 채굴기들이 땅에 떨군 광석이었다.

> 바닥의 광석이 제 출구를 스스로 막고 있었다.

**조치 2.** `seat_ok` 가 두 가지를 갈라 답하게 했다 — 「지금 당장 놓을 수
있다」와 「주우면 놓을 수 있다」. 후자면 `sweep` 태스크로 먼저 줍는다. 줍는
것은 치우는 일이면서 동시에 거두는 일이다. 광석은 광석이다.

**결과 2.** 화로 자리 0 → 20곳.

**판단 3 — 자급쌍은 창고다.** 석탄 위 채굴기 48대가 전부 연료칸 50/50 으로
꽉 차 있었다. 출력 상자가 없으니 캔 석탄이 갈 곳이 연료칸뿐이고, 한 칸이
차면 멈춘다. 덜어 가는 것이 사용법이고, 덜어 가는 것이 곧 되살리는 일이다.

**결과 3.** 「석탄 어디 있나」 조회가 62곳을 돌려준다 — 상자 14 + 자급쌍 48.
캐지 않고 꺼내 쓸 수 있는 석탄이 1,200개 생겼다.

---

## 30회차 부록 — 파일을 가른 것이 버그를 하나 드러냈다

Lua 를 아홉 모듈로 가르기 전에 「누가 누구를 부르는가」를 쟀다. 그 과정에서
`blocking` 원격이 `blocks_lane` 을 선언보다 417줄 위에서 부르고 있는 것을
봤다. Lua 의 local 은 그 줄 아래에서만 보이므로 조용히 nil 이 되고, 부르는
순간에야 터진다. 그 원격은 아무도 안 불러서 **한 번도 안 터졌고**, 「길
치우기」 기능은 없는 채로 돌고 있었다.

이 저장소에서 세 번째다(TENDED, init_status_names, blocks_lane). 그래서
`tests/lua_order.py` 를 붙였다. 지금은 두 가지를 본다 — 선언보다 먼저 쓰는
이름, 그리고 남의 모듈 이름을 require 없이 쓰는 것.

> 파일을 가르는 일은 정리가 아니라 검사다. 경계를 그으면 경계를 넘는 것이
> 보인다.

---

## 31회차 — 발전소는 멀쩡했다. 기지가 두 동강 나 있었을 뿐이다

**실측** (정기점검).

```
채굴기 92대 중 29대 가동  (지난 점검 21대)   눈먼 채굴기 19 -> 13
화로   59대 중 11대 가동  (지난 점검  4대)   도망 화로   34 -> 28
요원 넷 전부 기지 22타일 안                   (지난 점검 140~322타일 밖)
창고: 돌 10,121  구리광석 1,268  석탄 265  철광석 65
```

앞 회차의 조치가 듣고 있다. 그런데 랩은 여전히 0대 가동이다.

**판단.** 전력을 재보니 발전소 자체는 멀쩡했다 — 보일러 증기 만땅, 기관
일곱 대 정상, `power_faults` 깨끗. 그런데 0W다. 좌표를 보고 알았다:

```
발전소 (-96, -70)        기지 (77, 24)        196타일
기지에서 가장 가까운 물 (-45, -47)            141타일
```

캐고 녹이는 곳과 전기를 만드는 곳이 서로 남이었다. 발전기가 안 도는 이유는
고장이 아니라 **쓰는 사람이 저쪽에 없어서**였다. 보일러의 `full_output` 은
「막혔다」가 아니라 「아무도 안 가져간다」는 뜻이다.

> 같은 상태값이 고장일 수도 있고 한가함일 수도 있다. 그 둘을 가르는 것은
> 그 기계가 아니라 그 옆에 무엇이 있느냐다.

**조치.** 물이 141타일 밖이니 발전소를 옮길 수는 없다. 전봇대를 길게 깐다.

  - `power_reach(x,y)` — 발전기가 **붙어 있는** 망의 가장 가까운 전봇대까지.
    전봇대끼리만 이어진 섬은 아무리 커도 전기가 없다.
  - `pole_route(...)` — 그 사이 자리. 막힌 칸은 두 칸까지 비켜보고, 비켜도
    안 되면 거기까지만 준다. 반쯤 깐 줄도 다음 사람이 이어 깐다.
  - `run_wire` — 한 번에 여덟 개씩. 스물일곱 개를 한 번에 들고 가려면 그만큼을
    먼저 만들어야 하고, 그동안 아무도 아무것도 못 한다.

**결과 (배포 직후 확인).** 가장 가까운 전기 있는 전봇대는 (-51.5,-14.5),
132타일. 자리 여덟 개가 바로 나왔다: (-44,-12) (-38,-10) (-31,-7) (-24,-5)
(-18,-3) (-11,0) (-4,0) (1,3). 전부 열아홉 개면 닿는다.

순찰에서는 «가까이 있는 것이 어긋난» 네 경우(다리/파이프/전봇대/연료)를 먼저
보고, 그것으로 안 될 때만 이 긴 줄을 깐다. 싼 것부터 보는 순서는 그대로다.

---

## 32회차 — 구역을 나누고, 그 사이를 벨트가 잇는다

**지시** (사용자, 연달아).

> "지금은 채굴지에서 채굴/조립 을 다하는데, 이렇게말고 구역을 나눠서
> 채굴 / 운반 / 조립 등으로 심시티하는게 좋응ㅁ."
> "채굴한 광석들은 왠만하면 운반할때아니고서야 인벤토리에 두고있지말것."
> "벨트로 운반하게 해봐"

**판단.** 지금까지 «구역»이라는 개념 자체가 없었다. 채굴기 앞에 화로를 놓고,
화로 옆에 조립기를 놓고, 조립기 옆에 랩을 놓았다. 기준이 늘 «바로 옆»이라
광맥 위에서 캐고 녹이고 만드는 일이 전부 벌어졌다.

**조치.** 파일 둘을 새로 냈다. 새 책임이니 새 파일이다.

```
mods/ai-bridge_0.3.0/zones.lua   mine / smelt / craft - 무엇을 어디서 하는가
mods/ai-bridge_0.3.0/belts.lua   광석 길 - 세 토막 중 아직 없는 것만
bridge/crew/hauling.py           운반이 독립된 역할이 되었으므로
```

버너 시대에 벨트를 쓰려면 세 토막이 필요하다. 셋 다 있어야 한 토막이라도
쓸모가 있다:

| 토막 | 무엇 | 빠뜨리면 |
|------|------|----------|
| 올라타는 곳 | 사람이 상자에서 벨트에 얹는다 | 아무것도 안 흐른다 |
| 길 | 벨트 줄 (전기 불필요) | 이을 것이 없다 |
| 내리는 곳 | 벨트 옆 버너 인서터 → 화로 | **광석이 줄 끝에 쌓이기만 한다** |

세 번째가 함정이다. 벨트는 스스로 화로에 넣지 못한다.

**헛짚은 것 셋.** 전부 같은 뿌리다 - 기준이 그때그때 달라지면 아무것도 안
쌓인다.

1. **조립 구역이 채굴 구역 한복판에 잡혔다.** 「빈 땅」만 봤기 때문이다.
   그 순간 건물이 없었을 뿐, 거기는 곧 채굴기가 설 땅이다.
   → 구역은 «지금 비어 있는가»가 아니라 «누구 땅인가»로 정한다.

2. **길을 캐는 구역에서 출발시켰더니 첫 칸부터 막혔다.** 실측:

   ```
   시작점 (70,42) 주변 169칸 중 열린 칸  60   그나마 서로 끊겨 있음
   목적지 (90,24) 주변 169칸 중 열린 칸 161
   ```

   채굴기 밭 한복판은 원래 그렇다.
   → 트인 쪽에서 파고 들어간다. 닿는 데까지만 깔고 남은 몇 걸음은 사람이
   걷는다. 「끝까지 안 닿으니 아예 안 깐다」보다 언제나 낫다.

3. **벨트 마흔 칸을 깔고도 「트렁크 0칸」.** 너비 우선은 같은 거리의 길이
   여럿일 때 아무거나 고르고, 그 사이 지형도 바뀐다. 부를 때마다 다른 길이
   나왔고 깐 벨트는 늘 «새 길 위에 없는» 벨트가 되었다.
   → 길은 한 번 정하면 남는다(`storage.ore_line`).
   → 그리고 「길 위에 있는가」를 물을 수 있게 되자, 버려진 마흔 칸을 걷어와
     다시 쓰는 일감도 생겼다. 길을 정해두는 것은 낭비를 막는 일이기도 하다.

**그리고 규칙 하나가 다른 규칙을 껐다.**

「광석을 가방에 두지 말라」를 넣자 손에는 늘 열 개뿐이 되었다. 그런데 화로를
먹이는 조건이 `snap.have(ore) < SMELT_BATCH` 였다 - 가방을 본다. 그래서
창고에 철광석 886개가 쌓인 채 화로 쉰네 대가 「재료 없음」으로 서 있었다.

> 규칙이 틀린 게 아니라, 규칙이 바뀌었는데 그것을 가리키는 조건이 그대로였다.

`ore_within_reach` - 가방이 아니라 세상을 본다. 손에 있으면 그대로 넣고,
없으면 그 화로에서 가장 가까운 상자에서 꺼내오는 단계를 앞에 붙인다.

**실측 (이 회차 끝).**

```
채굴기 149대 중 82대 가동     전기망까지 3타일 (한때 196)
화로    72대 중 16대 가동     광석 길 109칸 남음, 길 밖 벨트 40칸 회수 대기
창고: 돌 16,129  철광석 886  석탄 145  구리광석 139
연구 3개 (gun-turret 진행 중)
```

---

## 33회차 — 설계가 여덟이면 길도 여덟이다

**실측.** 사용자가 사진 한 장과 함께 물었다: "이건뭐람". 벨트가 뱀처럼
휘감긴 미로였다.

```
세상의 벨트  548
길 밖 벨트   200 (조회 상한, 실제로는 더)
ore: todo 20  남은 150  이미 {trunk:0, lane:0, arms:0, out:0, pick:0}
```

**판단.** 내가 만든 것이다. 32회차에 「얼려둔 길 위에 뭔가 서 있으면 다시
찾는다」를 넣었는데, 그 다시 찾기가 `ore_line` 을 부를 때마다 «길 전체»를
새로 냈다. `ore_line` 은 여덟이 각자 매 순찰마다 부른다. 그러니 순찰
한 번에 새 길 여덟 개가 나왔고, 여덟이 각자 스무 칸씩 깔았다.

철판 팔백 개어치다.

**조치 1 — 고칠 곳만 고친다.** 막힌 «구간»만 찾아, 그 앞의 마지막 성한 칸과
뒤의 첫 성한 칸 사이를 우회로 잇고 나머지는 그대로 둔다(`repair`).

**조치 2 — 설계는 하나.** 사용자가 답을 먼저 말했다: "심시티 자체는 반장이
설계해서 각 에이전트들한테 건설을 위임하는게 좋을듯."

미로의 원인이 정확히 그것의 반대였다. 설계가 여덟이면 길도 여덟이다.
그래서 셋으로 쪼갰다 — 설계(`flow_plan`), 배분(`claim_work`), 건설(`lay_line`).
짓는 사람은 「길이 어디냐」를 다시 묻지 않는다. 「내 몫이 어디냐」만 묻는다.

**덤.** 예전에는 열쇠가 흐름마다 하나여서 한 번에 한 사람만 길을 깔았다.
몫이 나뉘면 같은 칸에 둘이 설 일이 없으므로 셋이 동시에 붙을 수 있다.
넷째부터는 벨트 만들 철판이 모자라서 서로 굶긴다.

**교훈.** 이번 주 세 번째로 같은 모양이다.

> 여럿이 각자 판단하면 답이 여럿 나온다. 판단을 나누지 말고 «일»을 나눠라.

고칠 때마다 되새길 것: 이 함수를 «몇 명이 몇 번» 부르는가. `ore_line` 은
여덟 명이 매 순찰마다 부르는 함수였고, 나는 그 안에 「전체를 다시 계산한다」를
넣었다.

---

## 34회차 — 철밭이라 부르던 곳에 철이 한 톨도 없었다

**실측**. 사용자가 먼저 알아챘다: "지금 철이라고 인식되는게 철이아니라
돌인데?" 채굴기 열한 대에게 무엇을 캐느냐고 물었다.

```
( -55,  -4) 캐는것=stone  밑에: stone=4469
( -53,  -4) 캐는것=stone  밑에: stone=5923
...열한 대 전부 stone. 철 0.
```

광맥을 다시 쟀다.

```
철 광맥(붙어 있는 덩어리)   x[-56..-22] y[ -4..27]
돌 광맥                     x[-61..-44] y[-14.. 3]
겹치는 상자                 x[-56..-44] y[ -4.. 3]  <- 여기는 다 돌
```

**판단**. 홍수채움으로 「붙어 있는 것만 한 밭」은 33회차에 이미 고쳤고,
그 결과는 맞았다. 문제는 그 결과가 «상자»라는 것이다. 상자 안에 남의
광맥이 박혀 있으면 상자만 보고는 알 수 없다.

두께를 세는 `ore_under` 가 `type="resource"` 로 아무 광물이나 셌다.
돌 4469 를 「철 두께 4657」로 읽고 자리를 열한 칸 내놓았다.

「어느 광맥인가」는 이미 알아내 놓고 있었다(`want_name`). 테두리를 다시
재는 데만 쓰고 두께를 세는 데는 안 썼다.

> 알아낸 것을 가리키는 데가 없으면 알아낸 보람이 없다.

이 저장소가 여러 번 만든 모양이다 — `blind_drills`, `storage.ore_line`,
`wall["want"]`, 첫 대의 자리표, 죽은 `rebalance` import.

**조치**
- `ore_under` 가 «이 밭의» 광석 이름으로만 센다
- `want_name` 을 블록 밖으로 내고, 부르는 쪽이 이름을 주면 그것을 쓴다.
  가운데 칸에 물어 짐작하면 돌이 박힌 철밭에서 또 돌을 고른다
- `mine_seats` 가 밭 이름을 되돌려준다
- 검사 `thickness_is_this_ore`

**결과**: 커밋 `7ba73f0`. 돌 채굴기 일곱 대를 뜯어 진짜 철 몸통
(x[-50..-43] y=11..15, 매장 800 이상)으로 옮겼다. 열네 대가 전부
`iron-ore` 를 캔다. 상자에 철광석이 처음으로 들어왔다.

---

## 35회차 — 「못 놓는다」와 「치우면 놓는다」는 다른 말이다

**실측**. 사용자: "중간에 큰바위가 있는게 파괴할방법잇나?"

```
big-rock (-48.1, 14.5)  type=simple-entity
```

철 줄 한가운데였다. 2x2 자리 네 칸과 벨트 두 칸(-49,-48)을 덮었고,
벨트 아홉 칸을 주문했는데 일곱 칸만 깔렸다.

**판단**. 치울 수 있는데 아무도 치우지 않았다.

```
자리표      can_place_entity 가 거짓이면 「막힘」으로 세고 지나간다
blocking    우리 힘(force)의 것만 본다. 바위는 neutral 이라 안 보인다
```

`demolish` 는 힘을 안 가리고 `minable` 이면 뭐든 캔다. 바위를 캐면 돌과
석탄이 가방으로 들어온다 — 치우는 일이 곧 거두는 일이다. 길이 이미 있는데
그 길을 가리키는 것이 없었다.

벨트에서는 이미 한 번 갈라 놓았다 — `sweep`(줍고 깐다) / `lift`(걷고 깐다)
/ `blocked`(못 깐다). 같은 말이 자리표에는 없었다.

> 한 곳에서 배운 것을 다른 곳에 안 옮기면 같은 사고를 두 번 겪는다.

**조치**
- `plots.clearable()` — 자리를 덮은 것이 바위나 나무뿐이면 그 자리를
  내놓고, 치울 것을 `clear` 로 실어 보낸다. 치울 수 없는 것이 하나라도
  섞여 있으면 그냥 막힌 자리다
- `mining` 이 `clear` 를 받으면 세우기 전에 `demolish` 한다
- 검사 `rocks_are_clearable`

**결과**: 커밋 `93eb5a3`.

---

## 36회차 — 석탄 600개를 달라 했는데 1개를 받아 오고도 성공이었다

**실측**

```
부른 칸          (-70,15)
석탄 상자        가운데 (-69.5,15.5)  거리 0.707  석탄 1363
급유 인서터      가운데 (-70.5,14.5)  거리 0.707  석탄 1
```

**판단**. 거리가 «같다». 둘 다 「가지고 있다」 가산점 100을 받아 동점이
되었고, 먼저 나온 쪽이 이겼다. 급유 장치는 상자와 인서터를 한 칸 간격으로
세우므로 이 둘은 언제나 같이 잡히고, 언제나 동점이다.

같은 자리에서 이미 한 번 다쳤고 주석까지 달아 두었다(`tasks.lua` 505행).
그때 고친 것은 「무엇을 꺼낼지 알려준다」까지였다.

> 고르는 기준은 「가지고 있는가」가 아니라 «달라는 만큼 가지고 있는가»다.

**조치**: `target_entity` 가 `how_many` 를 받아, 달라는 만큼 가진 쪽에
100을 더 준다. `take` 가 `p.count` 를 건넨다. 검사 `take_wants_enough`.

**결과**: 커밋 `a6e00f6`.

---

## 37회차 — 순서를 안 지키면 열여섯 번 헛손질한다 (사람 쪽 실수)

**실측**: 구리밭 채굴기 16대 급유 명령이 전부 빗나갔다. 석탄 581개가
그대로 남았고 채굴기는 전부 `no_fuel`.

**판단**: 내가(반장) 급유를 채굴기가 «서기 전에» 넣었다. 대상이 없으니
`nothing with an inventory at ...` 로 열여섯 번 실패했다. 코드 문제가
아니라 순서 문제다.

자동 배차(`dispatch`)에는 이런 일이 없다 — 일감이 「세운 다음 먹인다」로
한 덩어리이기 때문이다. 사람이 손으로 몰 때만 생긴다.

**교훈**: 수동 운전에서 두 사람에게 앞뒤가 있는 일을 동시에 던지지 말 것.
앞사람이 끝난 것을 «게임에 물어» 확인하고 뒷사람을 보낸다.

---

## 21회차 — 아무도 죽지 않았다

**실측** (20회차 대비):

```
              20회차        21회차
사망           6 / 8         0 / 8
덮인 건물      38%           80%
포탑           55대          51대
진화도         0.2219        0.0536
```

포탑 수는 오히려 줄었는데 덮인 비율은 두 배가 됐다. 20회차의 문제가
밀도가 아니라 «사슬의 길이»였다는 뜻이다.

**시작**: 사용자의 두 마디에서 출발했다 - 「다른 유저들은 방어선 구축을
어떻게하는지 리서치해바」, 「벨트좀 체크해봐 뭔가 이상한데」.

### 리서치가 바꾼 것

위키(Enemies)가 말하길 벌레는 목표까지 최단 경로로 가며, 우회할 길이
있으면 «장애물을 거들떠보지 않고» 돌아간다. 그러면 커버리지 38%는
「38% 지켜짐」이 아니라 「62%로 가는 뒷문이 늘 열려 있음」이다.

이 한 줄이 세 가지를 바꿨다.

| 무엇 | 전 | 후 |
|---|---|---|
| guard 보고 | "95% - 지킬 만큼은 지켜졌다" | "아직 N채가 사거리 밖 (그만큼 문이 열려 있다)" |
| 포탑당 탄약 | 20발 + 창고 200 | 10발 + 창고 100 |
| 보급 상한 | 한 대에 100발 (= 철판 400) | 30발 |

탄약에 잠겨 있던 철판 1,400장이 포탑 열여섯 대 분이었다. 쏜 탄약은 그때
«한 발도» 없었다.

### 고친 고장들 - 세 계열

**1. 보고가 실제와 따로 놀았다**

- `craft ... wait: False` 를 「만들어졌다」로 읽음 (다섯 군데)
- `add_research` 의 false 를 안 보고 「연구 대기열」이라고 찍음
- 모드가 나무 한 그루를 놓치고 「막힌 것 없음」이라고 답함
- 테스트가 「6 rules checked」를 리터럴로 찍음 (실제로는 13)

**2. 재는 값이 목적과 어긋났다**

- 정렬 첫 항이 `covers > 0` - 한 번 참이 되면 영영 참이라 순서를 못 정함
- 배분이 「캔 비율」 - 쓸 데 없는 구리를 2,000장 녹임
- 부족 판정이 「쌓임」 - 운반이 바로 퍼가면 더미가 안 커져서 안 보임
- 매듭 판정이 「채굴기 수」 - 석탄이 없어서 석탄을 못 캠
- 자리표가 「격자」 - 어디가 비었는지 모름
- 일꾼 선택이 「한가함」만 - 120타일 밖 사람에게 명령이 감

**3. 규칙이 자기가 지키려던 것을 막았다**

- 방어선 게이트가 석탄까지 막음 → 꺼지는 일은 안 막게
- 연료 예외가 손을 하나만 남김 → 둘로
- 그 손을 「멈춘 채굴기 손보기」가 먼저 가져감 → 연료 부족 땐 보류
- 최소 더미(25)가 굶은 물건까지 막음 → 굶으면 안 따짐

### 좌표에서 세 번 틀렸다

1x1 벨트에서 「가운데를 찍어라」를 배웠고, 그것을 «0.5를 더한다»로
외웠다가 2x2 화로에서 정확히 반대로 틀렸다. 그다음엔 「한 점과 반경」
으로 묻는 것 자체가 크기를 안다는 가정임을 알았다.

    area = {{floor(x), floor(y)}, {floor(x)+1, floor(y)+1}}

이 질문은 크기를 몰라도 된다.

### 남은 것

재시작해야 반영되는 넷 - 두 노선이 같은 칸을 반대로 원하는 교착,
모드 막힘 판정, 제련 12→18열, 치수 통합(`plots.lua` 한 곳으로).

**교훈**: 이번에 고친 것 대부분은 「없던 기능」이 아니라 «이미 아는 것을
안 쓴 것»이었다. 주석이 「캔 것이 산더미면 모자란 것은 나르는 길이다」
라고 적어 두고도 그 판단에 쓰는 값은 쌓임뿐이었고, `unguarded()` 가
안 덮인 것을 세고 있었는데 자리 후보로는 안 썼다.

    아는 것을 쓰지 않으면 모르는 것과 같다.

## 21회차 - hotel 전사 (정찰, 2026-09-23)

- 어디서: 북쪽 (-40,-847). 둥지는 (-40,-746) 스피터 둥지 하나와 (13..27,-911..-925)
  둥지 5 + 웜 2 무리. 기지에서 776칸 / 950칸.
- 왜: 정찰은 «다리 끝»(40칸마다)에서만 주변을 봤다. -730 에서 한 번 보고
  (그때 -746 둥지 청크는 아직 안 생성됨), 다음 다리로 걸어가는 «도중»에 둥지가
  생성됐고, 캐릭터는 스스로 싸우지도 도망치지도 않으니 그대로 걸어 들어갔다.
  사용자: "hotel 캐릭터 적이랑 마주쳤고 공격도 당햇는데 왜 가만히잇음?"
- 고침: scout.py 가 걷는 도중에도 6초마다 70칸 안을 본다. 적이 보이면 걸음을
  끊고 집 쪽으로 120칸 물러난다.
- 규칙대로 살리지 않는다. 남은 정찰은 다른 사람이 «안개를 걷는» 식으로 한다.
  (사용자가 「방금은 예외적이니까 1번만」 살렸다.)

## 21회차 - hotel 두 번째 전사 (안개 걷기 귀환 중, 2026-09-23)

- 어디서: (-208,-512). 둥지 (-190,-509) 옆 20칸.
- 무엇을 하다: `fogwalk.py` 로 가장자리 28곳을 밟아 청크 531 -> 895. 동쪽 570칸
  웜, 북서 (-816,-98) 웜을 보고 세 번 물러난 뒤 «집으로 한 번에» 걸었다.
- 왜: 길찾기는 밝혀진 회랑을 따라간다. 회랑은 정찰 때 (-222,-528)→(-144,-542)
  를 지났고, 그때는 (-190,-509) 둥지 청크가 아직 생성 전이라 70칸 감시에 안
  걸렸다. 귀환 때는 생성돼 있었고, 귀환 걸음은 «감시 없이» 걸었다. danger.py
  가 (-725,-298) 에서 «집 쪽으로 120칸» 물렸는데 집 쪽이 곧 둥지 쪽이었다.
- 고침: (1) fogwalk 는 걷는 다리마다 `walk_watch()` 로 보며 걷고, 귀환은
  «밟았던 지점을 거꾸로 되짚어» 지점마다 AVOID(150) 안에 적이 생겼으면 건너뛴다.
  (2) 물러나는 방향은 집이 아니라 «가장 가까운 적의 반대쪽» (danger.away_from_foes,
  fogwalk 도 같은 것을 쓴다).
- 규칙대로 살리지 않는다. 남은 사람 7 (alpha..golf). 시체 (-208,-512) 가방은 비어 있다.
- 2026-09-23 사용자: "hotel캐릭터는 언제 어디가서 죽었데; 1번만 다시살려줘" -> 두 번째 예외로
  살렸다 (기지 안 (-13,17) 에 복귀). 이후로는 살리지 않는다.

## 21회차 - 서쪽 둥지 (-198,35) 포탑 밀기 (2026-09-23)

사용자: "기지서쪽에 매우 가깝게 적기지가 생성되엇음. 유의하고 공격준비"

- 무엇: 스피터 둥지 1 + 웜 5 (대형 1·중형 3·소형 1), 기지 중심에서 158칸, 서쪽
  둘레(-102)에서 95칸. 진화 0.51 에서 확장으로 생긴 것 (북 (8,-201) 무리도 같이).
- 어떻게: `creep.py`. 캐릭터는 비무장이고 창고에 포탑 277대 - 둘이 포탑 6대씩을
  두 열(끝 웜에서 14·16칸)에 «동시에» 세우고 탄약을 넣고 45칸 물러난다.
- 1파 (x=-178, 관통탄 106발): **중형 웜 2 + 둥지 격파**. 포탑 12대 전멸 - 대형 웜이
  19.5칸에서 일방적으로 때렸다 (사거리 38 vs 18).
- 2파 (x=-184, 보통 탄창): 대형 웜 13.5칸 안에 12대. 전멸, 웜은 멀쩡. hotel hp 77.
  까닭: 대형 웜 물리 저항 «10 감소» - 보통 탄 5x1.4=7 은 0, 관통탄 8x1.4=11.2 도
  1.2 씩. 지금 연구로는 총알로 못 죽인다.
- 남은 것: 대형 웜 (-197.5,40) + 소형 (-203,32) + 중형 (-209,34). 둥지가 없어 유닛은
  더 안 나온다 - 38칸 안에만 안 들어가면 된다. 포탑 24대·관통탄 106발 씀.
- 다음: 군용 팩 -> physical-projectile-damage-3 (관통탄 14.4-10 = 4.4/발, 340발) 뒤
  3파. 그때까지 서쪽 둘레는 그대로 지킨다.

## 21회차 - alpha 전사 (-167,32) (2026-09-23)

- 무엇을 하다: guard.py 의 «받침 포탑» 순번. 포탑 밀기 2파로 x=-184 에 잠깐 선
  포탑 열이 «이웃 없는 외로운 포탑»으로 잡혔고, guard 가 alpha 에게 그 곁 받침
  포탑 3대를 시켰다. 그곳은 대형 웜 (-197.5,40) 사거리 38 안이다.
- 왜 못 막았나: 작전 중 danger 루프를 꺼 두어 부르는 사람이 없었고, guard 는
  자리를 고를 때 «적 구조물 곁인지»를 안 봤다. 시체 (-167,32) 에 포탑 3·탄약 55.
- 고침: guard.buildable() 이 적 둥지·웜 45칸 안의 자리를 뺀다 (hostile_spots).
  작전 중에는 창고 손이 아닌 사람만 밀기에 쓴다. alpha 는 규칙대로 살리지 않는다.
  남은 사람 7 (bravo..hotel).
- 2026-09-23 사용자: "alpha도 예외적으로 부활시키고" -> 예외로 살렸다. 이후로는 살리지 않는다.

## 21회차 - alpha 두 번째 전사 (263,153) (2026-09-23)

- 무엇을 하다: 석유 관로 건설 (oil.py, 회랑 y=10 x 46..245). 동쪽 둥지 무리
  (120..142,74..92)의 순찰 바이터가 회랑에서 45~60칸 거리였다.
- 왜: 반사(reflex.lua)가 «공격자 반대쪽 45칸»으로 튀었다. 바이터는 사람보다
  빨라 따라붙고, 맞을 때마다 다시 반대쪽으로 - 여덟 번 튀며 남동으로 밀려
  (283,165) 둥지 곁에서 죽었다. danger 의 «적 반대쪽»도 같은 결함.
- 고침: 튀는 방향은 «가장 가까운 우리 포탑 곁»(220칸 안). 쫓아온 것을 포탑이
  쏜다. 포탑이 없을 때만 반대쪽. 회랑 건설은 포탑 사슬을 먼저 이어 «항상
  사거리 안»에서 한다 (corridor 단계). 살리지 않는다. 남은 사람 7.

## 21회차 - charlie 전사 (150,58) (2026-09-23)

사용자: "charlie는 왜 인벤토리에 자원이가득차있으며, 포탑엔 왜 총알을 안넣고,
왜 적기지를 가로질러왔지? 땅벌레는 원거리공격하는데"

- 가방: oil.fetch 가 순번마다 철 400·구리 150·강철 120·돌 20 을 «무조건» 챙겼다.
  안 쓴 것이 쌓여 가방이 찼고, 찬 가방은 관통탄 take 를 못 받아 사슬 포탑 27대 중
  19대가 탄약 0 이었다. -> 이번에 드는 만큼만 (COST 표).
- 길: 순번마다 창고로 되돌아갔고, (250,103) -> 창고 직선이 동쪽 둥지 무리
  (121..142, 74..92) 한복판을 지났다. 게임 길찾기는 적을 모른다. 중형 웜(30)에 맞고
  반사가 포탑(138,15)으로 튀었지만 그 전에 죽었다. -> route.py: 60칸 넘는 걸음은
  적 구조물 60칸 밖으로 경유점을 먼저 짠다 (orders.submit 에서 일괄).
- 세로 사슬 x=248 은 (284,164) 대형 웜 36칸 - 사거리 38 안. -> x=235.
- 살리지 않는다. 남은 사람 6 (bravo, delta, echo, foxtrot, golf, hotel).
- 2026-09-23 사용자: "alpha랑 charlie 부활시키고" -> 둘 다 예외로 살렸다 (alpha 3번째, charlie 1번째).

## hotel 세 번째 전사 (2026-09-24, 동쪽 둥지 밀기 2파)

- 어디서: (103..107, 76..79). 2파 포탑 열 x=112 (서는 곳 104,77) - 중형 웜 (132,77)에서 20칸.
- 왜: 열을 세우는 동안 웜 산탄에 맞음(hp 250 -> 157). 반사가 «가장 가까운 포탑 곁»
  (110,80)으로 보냈는데 그 포탑은 웜 사거리(30) «안»에 있었다 - 밀기 열은 애초에
  웜 곁에 세우는 것이라, 밀기 중에는 «포탑 곁으로»가 곧 «웜 곁으로»다.
- 그 전 1파는 됐다: 포탑 11/12, 구조물 8 -> 6. 2파에서 4/12 세운 채 hotel 전사, 밀기 접음.
- 살리지 않는다. 남은 사람 7 (alpha, bravo, charlie, delta, echo, foxtrot, golf).
- 고칠 것 (다음 사람에게): 출정 중(detached) 반사는 «후퇴 지점(열에서 45칸 뒤)»으로,
  포탑 곁이 아니라. creep.py 가 retreat 를 모드에 알려 줘야 한다.
- 2026-09-24 사용자: "hotel 다시 살리고" -> 예외로 살렸다 (hotel 3번째). 같은 죽음을 막으려고 반사가 적 구조물 45칸 안 포탑을 피난처로 안 고르게 고쳐 배포했다 (reflex.lua SAFE).

## bravo 전사 (2026-09-24, 회랑 무리 (198..224, 125..141) 밀기 2파)

- 어디서: (183,133) 에서 소형 웜에 맞고, 반사가 고른 «피난처» (248,101) 로 뛰다 (192,128) 근처에서.
- 왜: 바로 앞에 고친 반사(reflex.lua SAFE)는 «적 구조물 45칸 밖 포탑»만 봤다. (248,101) 은
  둥지 «건너편» 동쪽이었고 탄약 없는 옛 사슬 포탑이었다 - 가는 길이 둥지 한복판을 지났다.
  내(Claude) 수정의 결함이다.
- 고침: 피난처 포탑은 탄약이 있고 · 적 구조물 45칸 밖이고 · 공격자의 반대쪽이고 · 가는 길
  (맞은 자리 16칸 뒤부터)이 적 구조물 30칸 안을 안 지나는 것만. 없으면 공격자 반대쪽 45칸.
- 그 전까지 오늘 밀기 네 무리(동 8 · 북 5 · 모퉁이 5 · 회랑 3/8)는 사망 없이 끝났다.
  회랑 무리는 둥지 하나 (224,140) 남음.
- 살리지 않는다. 남은 사람 7 (alpha, charlie, delta, echo, foxtrot, golf, hotel).

## echo 전사 (2026-09-24, 석유 관로 공사 뒤)

- 어디서: (140,106). 관로 4순번을 마치고 (244,97) 에서 바이터 무리 20 을 만나 danger 가
  집으로 불렀고, 돌아오는 길에 중형 바이터에 따라잡혔다. 반사는 90칸 북쪽 (138,15)
  포탑으로 보냈다 - 바이터가 사람보다 빠르다.
- 왜 무리가 거기 있었나: 회랑 둘레 포탑이 비어 있었다 (석유 사슬 45대 중 29대 탄 0).
  사슬은 «세울 때 넣은 것»이 전부였고 아무 고리도 채우지 않았다.
- 고침: (1) 총·탄이 있으면 17칸 안의 적을 «늘» 쏜다 (reflex.lua tick - 전에는 맞은 뒤 6초만,
  echo 사망 직후 배포). (2) ammo_run.py - 구역 포탑을 돌며 30발까지 채운다.
- 살리지 않는다. 남은 사람 6 (alpha, charlie, delta, foxtrot, golf, hotel).
