-- Custom SQL migration file, put your code below! --
-- 잭팟 풀의 단위를 코인 -> 1/100 코인으로 바꾼다.
-- 코인 단위로 들면 작은 베팅의 1% 적립이 반올림에 먹혀 베팅 레벨마다 실효 적립률이 달라졌다
-- (10 -> 0%, 50 -> 2%). 100배 단위로 두면 모든 레벨에서 정확히 1%가 쌓인다.
-- 값 자체는 그대로다: 25,000 코인 -> 2,500,000 (= 25,000.00 코인).
-- 마이그레이션 저널이 이 파일을 한 번만 실행하므로 곱셈이 중복 적용되지 않는다.
UPDATE "jackpot_pool"
SET "pool" = "pool" * 100,
    "seed" = "seed" * 100,
    "updated_at" = now()
WHERE "id" = 1;
