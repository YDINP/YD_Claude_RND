/**
 * 엔진 전체가 공유하는 상한값. **여기가 유일한 정의 지점이다.**
 *
 * 시뮬레이터와 감사 도구가 각자 상수를 들고 있으면 값이 어긋나는 순간
 * "엔진은 허용하는데 도구는 예외" 같은 상태가 된다. 그래서 한 곳에서만 내보낸다.
 */

/**
 * 한 라운드(유료 스핀 1회 + 거기서 파생된 프리스핀 전부)가 낳을 수 있는 프리스핀 상한.
 *
 * **엔진이 캡으로 강제한다.** 리트리거가 이 값을 넘기려 하면 넘는 만큼을 잘라내고
 * `freeSpinsCapped` 피처를 남긴다. 예외를 던지지 않는 이유는, 실제 스핀 경로에서
 * 예외는 곧 유저의 라운드가 통째로 날아간다는 뜻이기 때문이다.
 * 라운드를 끝낼 수 있게 잘라내고 사실을 기록하는 쪽이 안전하다.
 *
 * 시뮬레이터 쪽 루프는 이 캡이 지켜지는지 확인하는 방어적 단언으로만 이 값을 쓴다.
 */
export const MAX_FREE_SPINS_PER_ROUND = 10_000
